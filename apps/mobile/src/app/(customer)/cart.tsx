import { View, Text } from 'react-native';

export default function CartRoute() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="font-headline text-lg">Cart (Coming Soon)</Text>
    </View>
  );
}
