# Food Delivery System — User Stories & Acceptance Criteria (MVP / Release 1)

---

## Document Control

| Field | Detail |
|---|---|
| Document Title | Food Delivery System - User Stories and Acceptance Criteria |
| Version | 1.1 |
| Date | May 24, 2026 |
| Status | Submission Polished - Content Preserved |
| Document Owner | Business Analysis Team |
| Scope | Existing user stories, acceptance criteria, QA mappings, dependencies, and traceability for the SoLi Food Delivery Platform |
| Architecture Style | Modular Monolith |
| Review Mode | Document polishing only; no story or acceptance-criteria re-authoring |

## Revision History

| Version | Date | Author | Description |
|---|---|---|---|
| 1.0 | January 28, 2026 | Development Team | Initial user stories and acceptance criteria baseline |
| 1.1 | May 24, 2026 | Documentation Review | Submission formatting, document-control additions, reference cleanup, and trace-readiness review while preserving existing content |

## Table of Contents

- [Document Control](#document-control)
- [Revision History](#revision-history)
- [Document Scope and Preservation Note](#document-scope-and-preservation-note)
- [Source Documents](#source-documents)
- [Quality Attribute Taxonomy](#quality-attribute-taxonomy)
- [User Stories (Grouped by Role)](#user-stories-grouped-by-role)
	- [Planning Fields](#planning-fields)
	- [Customer](#customer)
	- [Restaurant Partner](#restaurant-partner)
	- [Shipper (Delivery Personnel)](#shipper-delivery-personnel)
	- [System Administrator](#system-administrator)
	- [Platform / Internal](#platform--internal)
- [Notes (configuration-based measurability)](#notes-configuration-based-measurability)
- [Traceability Matrix](#traceability-matrix)
	- [Matrix 1: User Story to UC](#matrix-1-user-story--uc)
	- [Matrix 2: UC to SRS to ASR to QA](#matrix-2-uc--srs--asr--qa)
	- [Matrix 3: ASR to ADD to ADR](#matrix-3-asr--add--adr)
	- [Matrix 4: Code to Requirement to Architecture](#matrix-4-code--requirement--architecture)
	- [Matrix 5: ADR to SAD View](#matrix-5-adr--sad-view)
	- [Matrix 6: Orphan Audit Report](#matrix-6-orphan-audit-report)
- [Final Validation Summary](#final-validation-summary)
- [Submission Readiness Checklist](#submission-readiness-checklist)
- [Glossary and Acronyms](#glossary-and-acronyms)
- [References](#references)
- [Final Submission Statement](#final-submission-statement)

## Document Scope and Preservation Note

This submission-polished version preserves the existing user stories, story IDs, acceptance criteria, QA mappings, priorities, dependencies, business meaning, and trace relations. The review scope is limited to document structure, Markdown consistency, reference organization, trace-status readability, and DOCX conversion readiness.

## Source Documents

This document derives MVP user stories from:
- [Business_Rules.md](Business_Rules.md)
- [Food_Delivery_Vision_and_Scope.md](Food_Delivery_Vision_and_Scope.md)
- [SRS_FoodDelivery.md](SRS_FoodDelivery.md)
- [ASR-ADD-SAD/14 Quality Attribute.md](ASR-ADD-SAD/14%20Quality%20Attribute.md)

## Quality Attribute Taxonomy

Quality Attributes (QAs) referenced use the course taxonomy: **Usability, Performance, Availability, Reliability, Scalability, Security, Interoperability, Manageability, Supportability, Testability, Maintainability, Flexibility, Conceptual Integrity, Reusability**.

> Note on format: Each story ends with an explicit QA list: `[QAs: ...]`.

---

## User Stories (Grouped by Role)

### Planning Fields

- **SP (Story Points)**: Fibonacci scale (1, 2, 3, 5, 8, 13) for relative effort.
- **Priority (MoSCoW)**: `Must` (MVP core), `Should` (MVP important), `Could` (post-MVP), `Won’t` (explicitly deferred).

---

## Customer

| ID | User Story | Acceptance Criteria | Quality Attributes | Justification | SP | Priority (MoSCoW) | Key Dependencies / Notes |
|---|---|---|---|---|---:|---|---|
| US-1 | **US-1 — Customer Registration/Login**: As a **Customer**, I want to **register and log in to the app**, so that **I can place and track orders with my profile**. **[QAs: Security, Usability, Availability, Reusability]** | - Given I am not logged in, when I submit valid registration details, then my account is created and I can log in immediately.<br>- Given I enter invalid credentials, when I attempt to log in, then I am denied access and shown a clear error without exposing sensitive details.<br>- Given I am logged in, when I update my basic profile fields (e.g., display name, phone number), then changes are saved and reflected on my next app launch.<br>- Given the auth service is unavailable, when I attempt to log in, then I receive a retryable error and the app does not crash. | - **Security** (OWASP ASVS L2 checks; auth required; no sensitive error leakage)<br>- **Usability** (first-time login/register completion ≤ 2 minutes in usability test; SUS ≥ 80)<br>- **Availability** (auth endpoints monthly uptime ≥ 99.9%; graceful retry on outages)<br>- **Reusability** (shared auth/session module reused across Customer/Restaurant/Shipper/Admin channels) | - **Security**: Login is the gatekeeper for customer identity and order ownership; if the system cannot resist unintended actions (e.g., unauthorized access), attackers can take over accounts, view personal data, and place fraudulent orders—directly harming trust, retention, and causing support/financial loss.<br>- **Usability**: Registration/login sits at the top of the conversion funnel; confusing validation or unclear errors increases drop-off before the first order and reduces repeat usage, so the flow must be straightforward and self-correcting under normal user mistakes (wrong password, missing fields).<br>- **Availability**: Authentication unavailability blocks every revenue-generating action (browse → add to cart → checkout requires identity); even short downtime translates into immediate lost orders and churn because users cannot enter the system when intent is high.<br>- **Reusability**: Authentication/identity should be implemented as a shared subsystem (account creation, login, token/session handling, standardized error responses) that can be reused across customer app, restaurant portal, shipper app, and the admin dashboard. Reuse avoids duplicating security-critical logic in multiple codebases/services (where small differences become security gaps), accelerates future channel rollout, and keeps identity policy consistent across the platform. | 5 | Must | Foundation for identity, order ownership, and role-based actions. |
| US-2 | **US-2 — Unified Restaurant & Food Discovery**: As a **Customer**, I want to **search for restaurants and food items in one place**, so that **I can quickly find what I want to order**. **[QAs: Performance, Usability, Scalability]** | - Given I search by keyword, cuisine type, category, or tag, when I submit, then the response includes matching open and approved restaurants and matching available menu items together in a single result.<br>- Given I search using a text filter (keyword, category, or cuisine type), matching is accent-insensitive (e.g., "pho" matches "Phở", "banh mi" matches "Bánh Mì"); tag filtering uses exact matching.<br>- Given I search without a food-specific filter (no keyword, category, or tag), then food items are not returned; only the restaurant section is populated.<br>- Given I provide my location, then results are filtered to restaurants and items within my chosen radius (default 5 km) and each result includes its distance from my location; latitude and longitude must both be provided together.<br>- Given I do not provide my location, then results are returned without distance filtering and no location-related error is shown.<br>- Given results are returned, they are paginated and the response includes separate totals for matched restaurants and matched items.<br>- Given I select a restaurant result, then I can navigate to its detail view.<br>- Given I select a food item result, then I can navigate to its restaurant's menu to continue ordering.<br>- Note: Delivery zone validation is enforced at checkout, not at search time (BR-3). | - **Performance** (first page render ≤ 2 seconds; p95 search response ≤ 2 seconds)<br>- **Usability** (restaurant discovery task completion rate ≥ 90% in usability test; navigation predictable)<br>- **Scalability** (meets the above p95 targets at 2× projected peak browse/search load in load test) | - **Performance**: Discovery is a high-frequency interaction; if the first page and search responses are slow, customers abandon browsing and never reach checkout, reducing conversion rate and revenue. The defined time thresholds ensure the system responds within a measurable window for the core “find a restaurant” task.<br>- **Usability**: Restaurant discovery is the primary navigation path; unclear result presentation or hard-to-find actions increases cognitive load and misclicks, leading to drop-off and lower repeat usage. A simple, predictable browse/search UI directly supports retention.<br>- **Scalability**: As the marketplace grows (more restaurants/menus) and peak-time traffic increases, the system must handle additional load without degrading the response time targets; otherwise the same product experience collapses exactly when demand (and revenue opportunity) is highest. | 5 | Must | Depends on restaurant onboarding (US-10/US-18) and menu data (US-11). |
| US-4 | **US-4 — View Restaurant Availability**: As a **Customer**, I want to **see whether a restaurant/menu item is available**, so that **I do not waste time ordering unavailable items**. **[QAs: Reliability, Usability, Performance]** | - Given a restaurant is marked Closed/Unavailable, when I open it, then the UI clearly indicates unavailability and blocks adding items to cart.<br>- Given a menu item is marked Sold Out, when I view the menu, then the item is not addable to cart.<br>- Given availability changes while I browse, when the change is received, then the UI updates within a defined window (e.g., ≤ 10 seconds) or on refresh. | - **Reliability** (unavailable items never addable; state consistent across views/sessions)<br>- **Usability** (Closed/Sold Out communicated unambiguously; blocked actions include next-step guidance)<br>- **Performance** (availability state reflected to customer within ≤ 10 seconds of change or on refresh) | - **Reliability**: Availability is a correctness-critical signal; if it is wrong, customers invest time building carts that later fail, increasing abandonment, cancellations, and support contacts—reducing trust and repeat purchases.<br>- **Usability**: Clear “Closed/Sold Out” cues and blocked actions prevent confusion and repeated failed attempts, keeping the ordering journey smooth and protecting conversion.<br>- **Performance**: The value of availability depends on timely response; if updates are delayed beyond the stated window, customers act on stale state and experience failure, so the system must reflect changes within a measurable time period. | 3 | Must | Depends on partner availability controls (US-12). |
| US-5 | **US-5 — Single-Restaurant Cart Constraint**: As a **Customer**, I want my **cart restricted to one restaurant**, so that **checkout and fulfillment remain consistent**. **[QAs: Conceptual Integrity, Usability, Testability]** | - Given my cart contains items from Restaurant A, when I attempt to add an item from Restaurant B, then the system blocks it and explains options (clear cart or cancel) (BR-2).<br>- Given I choose to clear the cart, when I confirm, then all items are removed and I can add items from Restaurant B.<br>- Given I cancel, then the cart remains unchanged. | - **Conceptual Integrity** (cart invariant: exactly one restaurant per cart enforced client+server)<br>- **Usability** (block explains options; user can resolve in ≤ 1 prompt without confusion)<br>- **Testability** (automated tests assert add-from-2nd-restaurant always fails with expected reason) | - **Conceptual Integrity**: “One cart = one restaurant” keeps the overall design consistent across pricing, delivery radius, fulfillment, and payment rules; without this integrity, the system accumulates contradictory behaviors that create defects and operational edge cases.<br>- **Usability**: This rule interrupts a user action; if the system blocks silently or unclearly, customers feel the app is broken and abandon the order. Explicit explanation and options preserve conversion by guiding the user to a clear next step.<br>- **Testability**: A strict constraint must be enforced consistently; defining precise pass/fail criteria enables automated tests to catch regressions early, preventing mixed-restaurant carts from leaking into checkout and causing downstream failures. | 2 | Must | Enforced by cart (US-22) and checkout (US-7). |
| US-6 | **US-6 — Delivery Address Within Radius**: As a **Customer**, I want to **confirm my delivery address is serviceable**, so that **my order can be accepted and delivered**. **[QAs: Reliability, Interoperability, Usability, Reusability]** | - Given I enter a delivery address, when I validate it, then the system confirms whether it is within the restaurant’s delivery radius (BR-3).<br>- Given the address is outside the radius, when I attempt checkout, then checkout is blocked with a specific reason.<br>- Given the map/geocoding provider is unavailable, when I validate the address, then I receive an actionable error and can retry. | - **Reliability** (same input address+config yields same decision; “outside radius” always blocks checkout)<br>- **Interoperability** (geocoding/maps integration success ≥ 99% with timeouts/retries; actionable errors on outage)<br>- **Usability** (serviceability result shown with reason and next step; error messages are actionable)<br>- **Reusability** (deliverability module reused by proximity search, geo-scope enforcement, and checkout validation) | - **Reliability**: Serviceability is a fulfillment prerequisite; if the system inconsistently classifies addresses, it either accepts undeliverable orders (leading to cancellations/refunds) or rejects valid orders (lost revenue). Reliability here directly protects completion rate and operational stability.<br>- **Interoperability**: Address validation typically depends on external geocoding/maps; integration problems must be detected and handled explicitly. Without dependable interaction, the system risks wrong distance calculations and incorrect accept/reject decisions.<br>- **Usability**: When the system blocks checkout, customers need a clear explanation and next action (change address or choose another restaurant). Poor feedback causes repeated failures, frustration, and churn at the most sensitive step of the buying flow.<br>- **Reusability**: The deliverability check (address normalization → geocoding → distance computation → “serviceable/not serviceable” decision) should be a reusable platform module, not duplicated across features. The same logic is needed by proximity search (US-3), service area restriction (US-20), and checkout validation; reuse prevents inconsistent accept/reject outcomes caused by slightly different implementations and reduces repeated integration code around map providers. | 5 | Must | Depends on map/geocoding/distance capability; ties to BR-3 and service area policy (US-20). |
| US-7 | **US-7 — Checkout With COD or VNPay (MVP)**: As a **Customer**, I want to **place an order using COD or VNPay**, so that **I can complete purchases with my preferred payment method**. **[QAs: Security, Usability, Reliability, Interoperability, Flexibility, Reusability]** | - Given I have items in my cart, when I proceed to checkout, then I can select either COD or VNPay as the payment method (BR-4).<br>- Given I select COD, when I confirm the order, then an order record is created with status `Pending` and an order ID is returned.<br>- Given I select VNPay, when I confirm the order, then the system immediately creates the order with status `pending` and returns a VNPay redirect URL (`paymentUrl`) for the customer to complete payment (BR-4); the order advances to `paid` only after the backend receives a valid VNPay IPN callback.<br>- Given VNPay payment fails, is cancelled, or the payment window times out without a valid IPN confirmation, then the order is auto-cancelled and the customer sees a clear failure/cancellation state.<br>- Given I retry the same checkout with the same client idempotency key within a configured TTL (e.g., `ORDER_IDEMPOTENCY_TTL_MINUTES`), when the request reaches the backend, then the system returns the same order ID and does not create a duplicate order. | - **Security** (payment callbacks verified; auth required; no sensitive payment data leakage in logs/errors)<br>- **Usability** (checkout completion rate ≥ 90% in test; failure/cancel states are clear with retry path)<br>- **Reliability** (idempotency key prevents duplicates; exactly one order ID returned per key within TTL)<br>- **Interoperability** (VNPay integration handles success/failure/cancel deterministically; timeouts/retries configured)<br>- **Flexibility** (new payment provider can be added via provider abstraction without breaking checkout contract)<br>- **Reusability** (shared payment integration/state-transition layer reused across COD/VNPay and future methods) | - **Security**: Checkout is where money and order creation intersect; if the system cannot resist unintended actions (e.g., forged callbacks or tampered payment results), it enables fraud, chargebacks, and disputes that directly damage revenue and platform credibility.<br>- **Usability**: Payment is the most abandonment-prone step; unclear COD/VNPay choice, confusing failure states, or poor feedback increases drop-off and reduces completed orders. A clear, guided flow improves conversion and repeat purchasing.<br>- **Reliability**: Orders and payments must remain consistent over time; without reliable idempotency and state handling, customers can be charged twice or create duplicate orders, causing refunds, support costs, and long-term trust loss.<br>- **Interoperability**: VNPay introduces external interactions and callbacks; the system must handle the gateway’s responses predictably, or checkout becomes fragile and causes failed payments and lost sales.<br>- **Flexibility**: Payment methods evolve with market/partner needs; if checkout is not adaptable, adding a new provider becomes a risky redesign that delays business expansion and slows time-to-market.<br>- **Reusability**: Payment handling should be built as a reusable integration layer (provider abstraction, callback verification, idempotency, and standardized payment state transitions) rather than bespoke logic per payment method. This allows future providers (new gateways, wallet payments) and adjacent flows (refunds, dispute handling, settlement exports) to reuse the same components without duplicating security- and money-critical code, reducing inconsistency risk and accelerating feature delivery. | 13 | Must | Depends on cart (US-22), constraints (US-5), deliverability (US-6), and lifecycle (US-8). VNPay requires gateway integration and confirmation handling. |
| US-9 | **US-9 — Real-Time Order Status Updates**: As a **Customer**, I want to **receive real-time order status updates**, so that **I can track progress without repeatedly refreshing**. **[QAs: Availability, Performance, Interoperability, Reliability, Reusability]** | - Given my order status changes, when the backend publishes an update, then my app receives the update within a defined SLA (e.g., ≤ 5 seconds under normal conditions).<br>- Given the app is in the background or closed, when a critical status update occurs, then the system delivers a push notification via FCM (Firebase Cloud Messaging) and the app reflects the latest status on next open (FR-2.3).<br>- Given the real-time channel disconnects, when reconnect occurs, then the app resynchronizes status to the latest state.<br>- Given push/WebSocket providers are degraded, then the system falls back to periodic polling without breaking the app. | - **Availability** (real-time channel monthly uptime ≥ 99.9%; graceful fallback when degraded)<br>- **Performance** (status update delivery latency ≤ 5 seconds under normal conditions)<br>- **Interoperability** (push/WebSocket/poll adapters succeed ≥ 99% and support timeout/retry + fallback)<br>- **Reliability** (no missing/conflicting status transitions; reconnect resync yields latest state 100% in tests)<br>- **Reusability** (shared event/notification subsystem reused for customer/restaurant/shipper/admin updates) | - **Availability**: Tracking is a trust feature; if it is frequently unavailable, customers feel the platform is unreliable, increasing anxiety, complaints, and churn—especially during delivery windows when support load is highest.<br>- **Performance**: “Real-time” has a measurable meaning; updates delivered outside the SLA (e.g., > 5 seconds) become stale and drive manual refreshing and support contacts, weakening user experience and retention.<br>- **Interoperability**: Push notifications and WebSocket/polling mechanisms are external or infrastructural dependencies; robust interaction is required to deliver updates across device states (foreground/background) and network conditions.<br>- **Reliability**: Status must remain correct over time (no missing transitions, no conflicting states). If the system delivers wrong status or fails to resync after disconnects, customers lose confidence, disputes increase, and operational teams spend more time investigating “where is my order” incidents.<br>- **Reusability**: Real-time delivery should be implemented as a reusable notification/event subsystem (event model, routing rules, retries, and channel adapters for WebSocket/push/polling) that can be reused for restaurant alerts, shipper dispatch updates, and admin operational notifications. Reuse avoids building one-off delivery pipelines per feature, keeps delivery semantics consistent, and reduces duplication in the most failure-prone area (fan-out, retries, offline handling). | 8 | Must | Depends on lifecycle events (US-8) and updates from restaurant/shipper actions (US-13, US-16, US-17, US-23). |
| US-33 | **US-33 — Review & Rating (Restaurant/Shipper)**: As a **Customer**, I want to **rate and optionally review the restaurant and delivery personnel after my order is delivered**, so that **I can share feedback and improve marketplace trust/quality**. **[QAs: Usability, Reliability, Security, Supportability]** | - Given my order status is `Delivered`, when I open the delivered order details (or order history), then I can submit a rating (1–5) for the restaurant and (if a shipper was assigned) a rating (1–5) for the shipper.<br>- Given my order status is not `Delivered`, when I attempt to submit a rating/review, then the system blocks the action and shows a clear message that rating is only available after delivery (BR-7).<br>- Given I have already submitted a rating for this order and target (restaurant or shipper), when I attempt to submit again (including retries), then the system rejects the duplicate and returns the existing stored rating/review (one rating per order per target).<br>- Given I submit an optional text review, then it is validated (e.g., required rating present; no executable content) and persisted, and I can view my submitted rating/review for that order in my order history. | - **Usability** (submit rating/review in ≤ 30 seconds for ≥ 90% users in usability test; validation errors actionable)<br>- **Reliability** (ratings allowed only for `Delivered` orders; at most 1 rating per order per target; idempotent under retries; enforced 100% in tests)<br>- **Security** (only authenticated order owner can submit; input validated/sanitized; no stored-XSS when reviews are displayed)<br>- **Supportability** (review records store order ID, actor ID, target ID, timestamp, and correlation ID; queryable for investigation/moderation) | - **Usability**: Rating is a lightweight, post-fulfillment action; if it is hard to find or slow to submit, users skip it and the platform loses a key trust signal that supports decision-making for future orders. A fast, low-friction flow increases participation and supports the Vision success metric that depends on post-delivery reviews (SM-3).<br>- **Reliability**: Review integrity must hold over time: only completed (`Delivered`) orders should contribute feedback and duplicates must be prevented (one rating per order per target). If reliability is weak, ratings become noisy or gameable, reducing trust and causing operational disputes (“I was rated twice”, “rating before delivery”).<br>- **Security**: Ratings influence marketplace reputation and can be attacked (spoofed submissions, unauthorized rating, content injection). The system must resist unintended actions by ensuring only the authenticated order owner can submit feedback and that stored reviews cannot execute as code when displayed, protecting users and the platform’s credibility.<br>- **Supportability**: Reviews can trigger disputes and moderation needs (fraud claims, abusive content, “wrong order” complaints). Persisting useful context (who/what/when/which order, correlation ID) enables support/admins to investigate issues and resolve them consistently without manual data reconstruction. | 5 | Could | Depends on delivered status (US-17) and lifecycle enforcement (US-8). Aligns with Vision FE-9; note SRS mentions delivery-complete rating prompt as TBD in Release 2, so MVP can support manual rating via order history first. |
| US-22 | **US-22 — Shopping Cart Management (MVP)**: As a **Customer**, I want to **add/remove items and adjust quantities in my cart**, so that **I can review my order before checkout**. **[QAs: Usability, Reliability, Performance]** | - Given I am viewing a menu item, when I add it to my cart with a quantity, then the cart updates and the running total is recalculated.<br>- Given my cart contains an item, when I increase/decrease quantity or remove the item, then the cart updates immediately and totals reflect the change.<br>- Given I leave and reopen the app, when I return to the cart, then the cart contents remain available (within a configured persistence window) or I am clearly informed the cart was reset.<br>- Given my cart contains items from one restaurant, when I attempt to add an item from a different restaurant, then the system enforces the single-restaurant constraint as defined in US-5 (BR-2). | - **Usability** (cart edit flows complete with ≥ 90% success in usability test; clear feedback on each action)<br>- **Reliability** (cart totals/quantities remain consistent across sessions; no partial updates under retries)<br>- **Performance** (cart add/update/remove reflected to user within ≤ 1 second under normal conditions) | - **Usability**: Cart edits are frequent and directly influence purchasing decisions; if interaction is confusing or feedback is unclear, customers hesitate, make mistakes, and abandon checkout—reducing conversion.<br>- **Reliability**: The cart is the “source of truth” for what will be charged; inconsistent quantities/totals across actions or app reopen leads to incorrect orders and payment disputes, increasing refunds and support cost while damaging trust.<br>- **Performance**: Cart updates and recalculation must respond within a tight time window; sluggish updates make the app feel broken and interrupt the checkout momentum, which is strongly correlated with drop-off and lost revenue. | 5 | Must | Depends on menu/catalog (US-11) and rule enforcement (US-5). |
| US-34 | **US-34 — Customer View Order History**: As a **Customer**, I want to **view my past and active orders**, so that **I can track outcomes, reopen details, and access delivered orders for review**. **[QAs: Usability, Supportability, Performance]** | - Given I am authenticated, when I open order history, then I can see only my orders with status, time, restaurant, total, and payment summary.<br>- Given I select an order, then I can view details and status history.<br>- Given an order is delivered, then the order detail can be used as the entry point for rating/review submission. | - **Usability** (history/detail navigation is predictable)<br>- **Supportability** (status history helps support and customer self-service)<br>- **Performance** (history list is paginated and responsive) | Added by the traceability audit to close UC-10 as a direct user-story anchor. | 3 | Should | Maps directly to UC-10 and supports US-9 and US-33. |
| US-35 | **US-35 — Customer Cancel Order**: As a **Customer**, I want to **cancel an eligible order with a clear outcome**, so that **mistakes can be corrected before fulfillment progresses too far**. **[QAs: Reliability, Usability, Security, Supportability]** | - Given I own an order in a cancellable state, when I cancel it with a reason where required, then the order transitions to `cancelled` and affected parties are notified.<br>- Given the order is already beyond the customer-cancellable state, then cancellation is blocked with a clear message.<br>- Given the order was VNPay-paid, then the cancellation path records the refund/compensation requirement. | - **Reliability** (cancellation obeys the same lifecycle state machine)<br>- **Usability** (blocked states explain why)<br>- **Security** (only the order owner can cancel)<br>- **Supportability** (reason, actor, and timestamp are retained) | Added by the traceability audit to close UC-21 as a direct customer story instead of relying only on restaurant/admin cancellation stories. | 3 | Should | Depends on lifecycle enforcement (US-8), notifications (US-9), and payment compensation (US-7/US-29). |

---

## Restaurant Partner

| ID | User Story | Acceptance Criteria | Quality Attributes | Justification | SP | Priority (MoSCoW) | Key Dependencies / Notes |
|---|---|---|---|---|---:|---|---|
| US-10 | **US-10 — Restaurant Onboarding Approval**: As a **Restaurant Partner**, I want to **be approved by an Admin**, so that **I can start receiving orders**. **[QAs: Security, Manageability, Supportability]** | - Given I register as a restaurant partner, when submission completes, then my restaurant record is created with `isApproved: false` (pending approval) and cannot receive orders (BR-1).<br>- Given an admin approves me (`isApproved` set to `true`), then I can start receiving orders.<br>- Given an admin rejects me, then my restaurant remains inactive (`isApproved: false`) and I receive a reason code/message. | - **Security** (RBAC enforced for approve/reject; actions audited; no unauthorized activation paths)<br>- **Manageability** (pending queue supports filter/paging; decision takes effect within ≤ 1 minute)<br>- **Supportability** (reason codes + timestamps stored and retrievable for support investigations) | - **Security**: Approval is the gate that prevents unintended actors from entering the marketplace; if the system cannot resist unauthorized activation or identity misuse, fraudulent restaurants can receive orders, trigger disputes/chargebacks, and expose customer data—damaging platform trust and revenue continuity (BR-1).<br>- **Manageability**: Onboarding is an operational process; admins need built-in facilities to review applications, change status, and detect stuck/inconsistent cases at runtime. Without these controls, the platform cannot keep pace with partner growth and governance degrades into manual workarounds and slow time-to-revenue for legitimate restaurants.<br>- **Supportability**: Decision reasons, timestamps, and status history provide actionable information to diagnose “why can’t I go live?” issues and resolve partner complaints quickly. Without this, support must investigate ad hoc, increasing turnaround time and operational cost. | 5 | Must | Depends on admin access + approvals (US-25, US-18). |
| US-11 | **US-11 — Restaurant Menu Management**: As a **Restaurant Partner**, I want to **create and update menu items**, so that **customers can order current offerings**. **[QAs: Maintainability, Usability, Reliability, Performance]** | - Given I create a menu item with required fields, when I save, then it becomes visible to customers.<br>- Given I edit price/name/availability, when I save, then changes propagate to customer browsing within a defined window (e.g., ≤ 60 seconds).<br>- Given invalid fields (e.g., negative price), when I save, then the system rejects the update with validation errors. | - **Maintainability** (menu schema/validation changes require modification in ≤ 3 components; verified via PR checklist/dependency review)<br>- **Usability** (create/edit task completion rate ≥ 90% in usability test; validation errors actionable)<br>- **Reliability** (saved changes persist and match across restaurant/admin/customer views; no partial writes)<br>- **Performance** (menu changes propagate to customers within ≤ 60 seconds) | - **Maintainability**: Menu structures evolve (new item attributes, promo rules, availability logic). If changes require touching many modules/services, releases become risky and slow; maintainability keeps menu updates deliverable with bounded impact so restaurants can adapt their offerings without destabilizing the system.<br>- **Usability**: Menu edits are operational tasks; unclear workflows or non-actionable validation errors lead to wrong prices/descriptions and staff frustration. A clear create/edit experience reduces mistakes that later translate into customer complaints and refund requests.<br>- **Reliability**: Once a restaurant saves a change, it must persist and remain consistent across sessions and across customer views (no partial writes, no “old price still showing”). Reliability failures directly cause incorrect orders and disputes because customers decide based on what the app shows.<br>- **Performance**: The “≤ 60 seconds propagation” is the response measure; if the system responds too slowly, customers keep seeing stale menus and can place orders against outdated price/availability, increasing cancellations and support load. | 8 | Must | Enables customer browse + cart (US-2, US-22). |
| US-12 | **US-12 — Restaurant Real-Time Availability Control**: As a **Restaurant Partner**, I want to **mark items/restaurant as sold out/closed**, so that **new orders are prevented immediately**. **[QAs: Reliability, Performance, Usability]** | - Given I toggle a menu item to `Sold Out`, when saved, then customers cannot add it to cart (BR-8).<br>- Given I mark the restaurant `Closed`, when saved, then customers cannot place new orders and see the closed state (BR-8).<br>- Given the system is under peak load, when I update availability, then the update is applied within a defined SLA (e.g., ≤ 10 seconds). | - **Reliability** (Sold Out/Closed enforced 100% for new cart/checkout attempts; no inconsistent states)<br>- **Performance** (availability changes applied within ≤ 10 seconds under peak load)<br>- **Usability** (toggle state visible and confirmed; staff can change state with ≤ 2 interactions) | - **Reliability**: Availability is a hard operational constraint: if “Sold Out/Closed” is not enforced consistently, the system continues accepting orders that the kitchen cannot fulfill. That drives cancellations/refunds and harms restaurant ratings and platform credibility (BR-8).<br>- **Performance**: The SLA (≤ 10 seconds under peak load) defines the acceptable response time for applying an availability change. If performance is poor, a stale window remains where customers can still add/checkout unavailable items—creating avoidable order failures right at peak demand.<br>- **Usability**: Availability control is used during rush hours; the UI must make the current state and action outcome obvious so staff can change state quickly and confidently. Confusing toggles or hidden states lead to incorrect settings and immediate downstream customer impact. | 5 | Must | Drives customer availability visibility (US-4) and prevents invalid orders. |
| US-13 | **US-13 — Accept/Reject Incoming Orders**: As a **Restaurant Partner**, I want to **receive and accept orders**, so that **I can start preparation**. **[QAs: Availability, Performance, Usability, Reliability, Security]** | - Given an order is placed, when it is routed to my restaurant, then I receive a notification and can open the order details.<br>- Given a new order arrives, then the system generates a prominent auditory + visual alert that persists until acknowledged (FR-3.3).<br>- Given I accept a COD order (`pending` state), when I confirm it (T-01), then status becomes `confirmed` and the customer is notified within the defined SLA; for a VNPay order already in `paid` state, I confirm via T-04 to advance to `confirmed`.<br>- Given I do not confirm within a configured timeout (`RESTAURANT_ACCEPT_TIMEOUT_SECONDS`), when the timeout elapses, then the system auto-cancels the order (status becomes `cancelled`) with a system-generated reason, and the customer is notified.<br>- Given I need to decline an order, when I submit a cancel action with a required reason (T-03/T-05/T-07 depending on current state), then the order is cancelled and the customer is notified with the reason. | - **Availability** (restaurant order-inbox channel monthly uptime ≥ 99.9%)<br>- **Performance** (order notification delivered and accept/reject processed within ≤ 5 seconds under normal conditions)<br>- **Usability** (accept/reject action reachable in ≤ 2 taps; alert persists until acknowledged per FR-3.3)<br>- **Reliability** (no duplicate accepts; expiry triggers exactly once at `RESTAURANT_ACCEPT_TIMEOUT_SECONDS` with reason code)<br>- **Security** (only owning restaurant can view/act; all actions require auth and are audited) | - **Availability**: Incoming orders are revenue events; when the restaurant channel is down, orders cannot be received/acknowledged and will time out. That directly reduces order throughput and creates customer dissatisfaction when orders appear “ignored.”<br>- **Performance**: Kitchen execution depends on timely signals—notification delivery, accept/reject actions, and customer status updates must occur within defined windows. If performance lags, preparation starts late, ETAs become inaccurate, and cancellations increase.<br>- **Usability**: Staff operate under noise and time pressure; persistent alerts and low-tap accept/reject flows reduce missed orders and mistaken actions. If the UI is hard to use, operational errors rise and partners perceive the platform as unreliable.<br>- **Reliability**: The accept/reject/expiry rules must behave consistently over time (no duplicate accepts, no stuck pending, no missing expiry). Reliability failures create ambiguous order states that break preparation planning and downstream tracking.<br>- **Security**: Order intake exposes sensitive order/customer information and state transitions; the system must resist unintended access and manipulation so only the correct restaurant can see and act on its orders, preventing malicious interference and data exposure. | 8 | Must | Depends on checkout creating/routing orders (US-7) and lifecycle enforcement (US-8). |
| US-23 | **US-23 — Restaurant Update Preparation Status (MVP)**: As a **Restaurant Partner**, I want to **update the preparation status of accepted orders**, so that **customers and shippers have accurate readiness information**. **[QAs: Reliability, Usability, Performance, Security]** | - Given I have confirmed an order (status `confirmed`), when I start cooking, then I can mark it as `preparing` (T-06) and the customer receives an update within the defined SLA.<br>- Given an order is `preparing`, when it is ready to hand off to the shipper, then I can mark it `ready_for_pickup` (T-08); the customer receives an update and the order becomes visible in the available-for-pickup pool for shippers.<br>- Given an order is not in the expected prior state, when I attempt a status transition, then the system rejects the change as out-of-sequence (BR-7). | - **Reliability** (out-of-sequence transitions rejected 100% per BR-7; no missing/duplicate transitions under retries)<br>- **Usability** (status update is a single explicit action; current state and allowed next state are visible)<br>- **Performance** (status updates propagate to customer/shipper within ≤ 5 seconds under normal conditions)<br>- **Security** (only authorized restaurant staff can transition status; requests authenticated and logged) | - **Reliability**: Preparation status is used by both customers and shippers to coordinate pickup and expectation. If updates are incorrect or out-of-sequence, shippers arrive too early/late, customers lose trust in tracking, and operational exceptions increase (BR-7).<br>- **Usability**: The kitchen needs a fast, mistake-resistant status update flow. Clear state visibility and constrained actions reduce accidental mis-updates that would otherwise propagate confusion to customers and delivery personnel.<br>- **Performance**: The SLA is the response measure for how quickly the system reflects readiness changes. Slow propagation makes tracking stale, increases “where is my order” contacts, and causes avoidable cancellations.<br>- **Security**: Status transitions affect fulfillment and customer-visible state; the system must resist unintended actions so only authorized restaurant staff can update preparation status, preventing malicious or erroneous tampering. | 3 | Should | Depends on lifecycle (US-8) and customer tracking/notifications (US-9). |
| US-24 | **US-24 — Restaurant Cancel Order With Reason (MVP)**: As a **Restaurant Partner**, I want to **cancel an order with a clear reason**, so that **customers are informed and operations stay consistent**. **[QAs: Reliability, Supportability, Usability, Security]** | - Given an order is in `pending`, `paid`, or `confirmed` state, when I cancel it (T-03/T-05/T-07), then a cancellation reason is required and the system records the actor and timestamp; cancellation from `preparing` state or later is blocked for restaurants.<br>- Given an order is canceled by the restaurant, then the customer is notified immediately and sees the cancellation reason (FR-2.4).<br>- Given an order is already `Picked Up` or `Delivered`, when I attempt cancellation, then the system blocks the action with a clear message. | - **Reliability** (cancellation blocked after `Picked Up`; state + notifications consistent for every cancel)<br>- **Supportability** (reason code + actor + timestamp recorded 100% and queryable by support)<br>- **Usability** (clear block message when disallowed; reason selection required and confirm step explicit)<br>- **Security** (only owning restaurant can cancel; auth required; cancellation action audited) | - **Reliability**: Cancellation is a disruptive operation; it must obey lifecycle constraints every time (e.g., cannot cancel after pickup) and produce a consistent, recorded outcome. If reliability fails, the platform creates contradictory states that trigger disputes, incorrect tracking, and downstream operational errors.<br>- **Supportability**: Reason codes plus actor/timestamp form the minimum evidence needed to investigate incidents and answer customer complaints quickly. Without this information, support relies on manual reconstruction, increasing resolution time and cost.<br>- **Usability**: In busy restaurant operations, staff must understand exactly when cancellation is allowed, what will happen, and what the customer will see. Clear messages reduce repeated attempts and prevent accidental cancellations that harm revenue and ratings.<br>- **Security**: Canceling affects revenue and customer experience; the system must resist unintended actions so only the owning restaurant account can cancel its orders, preventing malicious cancellations and preserving integrity of order records. | 3 | Should | Depends on lifecycle constraints (US-8) and notification surface to customers (US-9). |
| US-36 | **US-36 — Restaurant Manage Promotions**: As a **Restaurant Partner**, I want to **create and manage restaurant-scoped promotions or coupons**, so that **I can run controlled discounts that checkout can validate safely**. **[QAs: Flexibility, Reliability, Security, Testability]** | - Given I own a restaurant, when I create or update a promotion, then scope, validity window, quota, discount type, and limits are validated.<br>- Given a customer applies a valid restaurant promotion at checkout, then the discount is previewed/reserved/confirmed consistently.<br>- Given quota is exhausted, expired, or outside scope, then checkout rejects the promotion with a clear reason. | - **Flexibility** (discount types can evolve through the promotion boundary)<br>- **Reliability** (reservation/confirmation prevents quota drift)<br>- **Security** (restaurant owner scope enforced)<br>- **Testability** (pricing and edge cases covered by deterministic engine tests) | Added by the traceability audit to close UC-23 as a direct restaurant story. | 5 | Should | Depends on promotion engine and checkout (US-7). |

---

## Shipper (Delivery Personnel)

| ID | User Story | Acceptance Criteria | Quality Attributes | Justification | SP | Priority (MoSCoW) | Key Dependencies / Notes |
|---|---|---|---|---|---:|---|---|
| US-14 | **US-14 — Shipper Onboarding Approval**: As a **Delivery Personnel (Shipper)**, I want to **be approved by an Admin**, so that **I can receive delivery jobs**. **[QAs: Security, Manageability, Supportability]** | - Given I register as a shipper, when submission completes, then my account is `Pending Approval` (BR-1).<br>- Given an admin approves, then I can toggle availability and receive dispatch requests.<br>- Given an admin rejects, then I cannot receive jobs and I receive a reason. | - **Security** (RBAC enforced for approve/reject; actions audited; no unauthorized activation paths)<br>- **Manageability** (pending queue supports filter/paging; decision effective within ≤ 1 minute)<br>- **Supportability** (rejection reason + timestamps stored and retrievable; consistent status history) | - **Security**: Approval is the gate for a role that can see delivery addresses and affect fulfillment state. If the system cannot resist unintended activation or identity misuse, malicious shippers can access sensitive data and tamper with order progress, driving fraud, safety incidents, and rapid loss of marketplace trust (BR-1).<br>- **Manageability**: Approval must work as an operational pipeline, not a one-off feature—admins need facilities to review a queue, change shipper status, and detect stuck cases fast. Weak manageability creates onboarding backlog, reduces active shipper supply during peaks, and directly lowers delivery success rate and revenue throughput.<br>- **Supportability**: Rejection reasons, timestamps, and status history are the diagnostic evidence for “why can’t I receive jobs?” and for resolving disputes consistently. Without these artifacts, support must reconstruct events manually, increasing resolution time, operational cost, and partner dissatisfaction. | 3 | Must | Depends on admin access + approvals (US-25, US-18). |
| US-15 | **US-15 — Shipper Availability Toggle**: As a **Shipper**, I want to **set myself available/unavailable**, so that **I only receive orders when I’m ready to deliver**. **[QAs: Usability, Reliability, Performance, Security]** | - Given I set status to `Available`, then I become eligible to receive dispatch requests within a defined SLA (e.g., ≤ 10 seconds).<br>- Given I set status to `Unavailable`, then I stop receiving new dispatch requests immediately (or within SLA).<br>- Given intermittent connectivity, when I change status, then the app confirms whether the change is synced or queued. | - **Usability** (toggle completion ≤ 2 interactions; state + sync outcome visible; errors actionable)<br>- **Reliability** (server is source of truth; state converges after reconnect; no contradictory availability states in consistency tests)<br>- **Performance** (eligibility change takes effect p95 ≤ 10 seconds under normal load; toggle API p95 ≤ 1 second)<br>- **Security** (only the shipper can toggle own availability; requests authenticated; replay-resistant; action audited) | - **Usability**: Toggling availability is a frequent, time-sensitive action (starting/ending a shift, pausing mid-route). The UI must make state and outcome unmistakable (available/unavailable; synced vs queued), otherwise shippers unintentionally accept jobs they can’t fulfill or miss jobs they intended to take—hurting delivery success rate and shipper earnings.<br>- **Reliability**: Availability is a persistent runtime state that must remain correct over time across restarts and flaky networks. If client/server states diverge, dispatch targets the wrong pool (offers sent to unavailable shippers or withheld from available ones), causing missed pickups, longer ETAs, and lower customer satisfaction.<br>- **Performance**: The ≤ 10 seconds SLA is the response measure for when eligibility takes effect. If propagation is slow, dispatch decisions are made on stale availability, increasing wasted offers and reducing throughput precisely during peak ordering windows.<br>- **Security**: Because availability affects assignment and income, the system must resist unintended actions (unauthorized toggles, replayed requests). Without this, attackers can sabotage a shipper’s availability or manipulate dispatch fairness, creating operational disruption and fraud risk. | 3 | Must | Needed before dispatch/assignment can be meaningful. |
| US-16 | **US-16 — Shipper Accept & Pick Up Order**: As a **Shipper**, I want to **claim an available order for pickup and mark it en route**, so that **the customer can track the delivery accurately**. **[QAs: Reliability, Security, Interoperability, Performance]** | - Given an order is in `ready_for_pickup` state, when I pick it up (T-09, self-assign), then the order is atomically assigned to me (shipperId recorded) and status transitions to `picked_up`; if another shipper claims it simultaneously, I receive a conflict response.<br>- Given I have picked up the order (status `picked_up`), when I mark it en route (T-10), then status transitions to `delivering`.<br>- Given my app cannot reach the server, when I attempt a pickup or en-route transition, then the system does not transition the order state and the app shows a retryable error message. | - **Reliability** (atomic assignment: ≤ 1 shipper per order; pickup confirmation idempotent under retries; no duplicate/missing transitions in tests)<br>- **Security** (only authenticated assigned shipper can accept/confirm pickup; requests tamper- and replay-resistant; actions audited)<br>- **Interoperability** (push/maps/GPS adapters success ≥ 99% with timeouts/retries; user-visible fallback when degraded)<br>- **Performance** (accept/assignment lock confirmed p95 ≤ 5 seconds; pickup confirmation processed p95 ≤ 5 seconds under normal load) | - **Reliability**: This story is defined by correct, durable state transitions over time: accepting must be atomic (no double-assignment) and pickup confirmation must be idempotent under retries (no duplicate/missing transitions). If reliability breaks, the system produces contradictory tracking (customer thinks it’s picked up when it isn’t, or two shippers attempt the same job), causing delays, cancellations, and operational escalations.<br>- **Security**: Accept/pickup actions are authoritative and expose sensitive order/customer information; the system must resist unintended access and manipulation so only the assigned shipper can accept and confirm pickup. Otherwise, jobs can be hijacked, pickup can be falsified, and customer/restaurant data can be exposed—creating fraud and reputational damage.<br>- **Interoperability**: Successful pickup depends on interacting with external systems (maps/GPS/navigation, push notifications). If these integrations fail or behave inconsistently, the system must still provide clear fallbacks; otherwise shippers miss pickup windows, spend more time per delivery, and platform efficiency degrades.<br>- **Performance**: Dispatch is time-bounded; accept and pickup confirmation must be processed within defined windows so the platform can lock assignment quickly and keep customer tracking current. Slow processing increases competing-accept races and offer expiry, and stale tracking is perceived as delivery failure. | 5 | Must | Depends on order dispatch/assignment logic and lifecycle (US-8). |
| US-17 | **US-17 — Shipper Confirm Delivery**: As a **Shipper**, I want to **confirm successful delivery**, so that **the order can be closed and metrics can be tracked**. **[QAs: Reliability, Security, Supportability]** | - Given I am assigned to the order and the status is `delivering`, when I confirm delivery (T-11), then status becomes `delivered` and is visible to customer/admin (BR-7).<br>- Given the order is not in `delivering` state, when I attempt to confirm delivery, then the system rejects the transition (BR-7).<br>- Given my app cannot reach the server, when I attempt to confirm delivery, then the system does not transition the order state and the app shows a retryable error message.<br>- Given delivery is confirmed, then the system records timestamp and actor identity for traceability. | - **Reliability** (delivery confirmation recorded exactly once; out-of-sequence transition rejected 100% per BR-7; update visible within ≤ 5 seconds under normal conditions)<br>- **Security** (only authenticated assigned shipper can confirm delivery; requests tamper- and replay-resistant; action audited)<br>- **Supportability** (actor identity + timestamp recorded 100%; failure outcomes include reason code + correlation ID for support) | - **Reliability**: Delivery confirmation closes the order and must remain correct over time: recorded exactly once, durable under retries/offline recovery, and visible consistently to customer/admin. If reliability fails, the platform can show contradictory truth (“not delivered” after a delivery, or “delivered” without a valid transition), driving refunds/disputes and corrupting KPIs and financial flows.<br>- **Security**: Marking an order delivered has financial and reputational impact; the system must resist unintended actions so only the assigned shipper can perform this transition. Without strong access control and request integrity, attackers or mistakes can prematurely close orders and enable fraud.<br>- **Supportability**: When issues occur in real use (delivery confirmation not updating, rejected due to out-of-sequence status, or sync/network failures), the system must provide information that is useful for identifying and fixing faults: who performed the action, when it happened, what the outcome was (success/failure), and a clear failure reason (reason code/message). Without these traces, diagnosis and remediation become manual, slow, and inconsistent—extending incident resolution time and increasing operational effort. | 3 | Must | Completes the end-to-end order flow; depends on lifecycle (US-8). |

---

## System Administrator

| ID | User Story | Acceptance Criteria | Quality Attributes | Justification | SP | Priority (MoSCoW) | Key Dependencies / Notes |
|---|---|---|---|---|---:|---|---|
| US-25 | **US-25 — Admin Dashboard Access & RBAC (MVP)**: As a **System Administrator**, I want to **securely access the admin dashboard with role-based permissions**, so that **only authorized actions can be performed**. **[QAs: Security, Manageability, Supportability, Reusability]** | - Given I am not authenticated, when I attempt to access the admin dashboard, then I am redirected to authentication and cannot view admin data (FR-4.1).<br>- Given I am authenticated, when I attempt an admin action, then the system enforces RBAC and denies actions I am not permitted to perform (FR-4.2).<br>- Given an authorization failure occurs, then the system provides a non-sensitive error message and records the attempt for investigation. | - **Security** (RBAC enforced for 100% admin endpoints; unauthorized requests return 401/403; no sensitive error leakage)<br>- **Manageability** (roles/permissions centrally managed; policy changes take effect within ≤ 5 minutes without code changes)<br>- **Supportability** (each deny records actor, required permission, outcome, and correlation ID; queryable by time/actor)<br>- **Reusability** (shared auth/session + RBAC middleware reused across all admin features) | - **Security**: The admin dashboard is a privileged control plane that exposes high-impact governance actions (partner approval, suspensions, order intervention, reporting). The system must resist unintended access and actions by enforcing authentication and RBAC on every admin request (not only at UI navigation). If authorization is bypassable or inconsistent, a compromised account or privilege misuse can trigger fraud, data exposure, and loss of operational control (FR-4.1/FR-4.2).<br>- **Manageability**: RBAC is also a management mechanism: it gives administrators practical means to delegate responsibilities, limit blast radius (least privilege), and evolve permissions as teams and processes change—without resorting to brittle workarounds like shared super-admin accounts or manual checklists that are hard to govern at scale.<br>- **Supportability**: When access is denied in real use (misconfigured roles, expired sessions, policy changes), admins and support need useful information to identify and fix faults: clear non-sensitive outcomes for the user plus recorded diagnostics (who attempted what, when, and why it was denied). Without these traces, incidents become guesswork and remediation time increases.<br>- **Reusability**: Authorization should be a reusable policy/middleware component shared across all admin endpoints (and ideally across service APIs), rather than re-implemented per feature. Reusing one RBAC enforcement layer keeps permission decisions consistent as new admin features are added (US-18..US-32), reduces duplicated security logic, and prevents “permission drift” where two modules accidentally interpret the same role differently. | 8 | Must | Foundation for all admin stories (US-18/19/26/27/28/29/30/31/32). |
| US-18 | **US-18 — Admin Approve/Reject Partners**: As a **System Administrator**, I want to **review and approve/reject restaurant and shipper applications**, so that **the marketplace remains trustworthy and safe**. **[QAs: Security, Manageability, Supportability]** | - Given a new application exists, when I open the admin dashboard, then I can view a queue of `Pending Approval` accounts (BR-1).<br>- Given I approve an application, then the decision takes effect immediately and notifies the applicant.<br>- Given I reject an application, when I submit the rejection, then a decision note/reason is required and is visible to the applicant on next authentication attempt (FR-4.6, FR-4.7).<br>- Given I lack sufficient permissions, when I attempt to approve/reject, then the system denies the action. | - **Security** (approve/reject requires RBAC; actions audited; 0 unauthorized status changes in tests)<br>- **Manageability** (pending queue supports paging/filter; decision takes effect within ≤ 1 minute)<br>- **Supportability** (rejection reason/note required; reason+actor+timestamp recorded 100% and queryable) | - **Security**: Approval/rejection is the marketplace gate (BR-1). The system must resist unintended approvals/rejections by enforcing authorization and preventing privilege misuse, because an unauthorized approval can onboard fraudulent partners who interact with customers and orders, creating fraud, safety incidents, and immediate damage to platform trust.<br>- **Manageability**: Partner onboarding is a continuous operational workflow. Admins need built-in means to manage the process deterministically (queue visibility, consistent state transitions, immediate effect of decisions) to avoid backlogs and “stuck” cases that reduce partner supply and slow growth during demand peaks.<br>- **Supportability**: When applicants dispute outcomes (“still pending”, “why rejected”), the system must provide useful information to identify and fix faults during use: decision notes/reasons, timestamps, and status history. Without this evidence, support must reconstruct events manually, increasing resolution time and making governance decisions difficult to explain or defend (FR-4.6/FR-4.7). | 5 | Must | Depends on secure admin access (US-25). Enables onboarding flows (US-10, US-14). |
| US-19 | **US-19 — Admin Monitor Order/Platform Health (MVP)**: As a **System Administrator**, I want to **monitor order statuses and basic operational metrics**, so that **I can detect issues early and support operations**. **[QAs: Supportability, Manageability, Performance]** | - Given I access the dashboard, when the system is healthy, then I can view current orders grouped by status and basic counts (e.g., Pending/Accepted/Delivering/Delivered).<br>- Given an order remains in the same status beyond a configured threshold (e.g., `Preparing` longer than `ORDER_STUCK_THRESHOLD_SECONDS_PREPARING`), when the threshold is exceeded, then it is flagged for review with a reason code indicating the threshold that was breached.<br>- Given the monitoring view loads, then it shows data updated within a defined freshness window (e.g., ≤ 60 seconds). | - **Supportability** (stuck flags include breached threshold + reason code; flagged order shows drill-down status history)<br>- **Manageability** (stuck thresholds are configurable without code; changes take effect within ≤ 5 minutes)<br>- **Performance** (dashboard data freshness ≤ 60 seconds; page load p95 ≤ 2 seconds for 1,000 active orders) | - **Supportability**: The dashboard should surface useful information to identify and fix faults during use: per-status counts, “stuck” detection, and the specific reason code/threshold that triggered the flag. These signals turn symptoms into actionable diagnostics and reduce time to isolate root causes.<br>- **Manageability**: The monitoring artifacts are also the administrator’s means to manage and tune the running system—prioritizing interventions, spotting bottlenecks, and adjusting detection thresholds/configuration based on observed behavior rather than guesswork.<br>- **Performance**: The monitoring view must respond within the stated freshness window (e.g., ≤ 60 seconds) to be operationally effective. If load/refresh is slow or data is stale, administrators miss early warning windows and small delays can cascade into cancellations and support incidents. | 5 | Should | Depends on order model + status history; complements US-28. |
| US-26 | **US-26 — Admin Search User Accounts (MVP)**: As a **System Administrator**, I want to **search user accounts by role and status**, so that **I can manage onboarding and operations efficiently**. **[QAs: Manageability, Supportability, Performance]** | - Given I open the user management view, when I filter by role (Customer/Restaurant/Shipper) and status (Pending Verification/Approved/Rejected/Suspended), then the results list matches the filters (FR-4.3).<br>- Given I select a user, then I can view their account details including current status and decision notes (where applicable).<br>- Given there are many users, when I search, then the system responds within a defined threshold (e.g., p95 ≤ 2 seconds for typical queries). | - **Manageability** (role+status filters and paging supported; results consistent with selected filters)<br>- **Supportability** (account details include status and decision notes/history; actions provide a correlation ID for follow-up)<br>- **Performance** (p95 search response ≤ 2 seconds for typical queries) | - **Manageability**: Role/status search is a primary administrative mechanism that provides practical means to manage onboarding and operations—quickly finding pending, rejected, or suspended accounts to take the correct next action instead of manual lookups (FR-4.3).<br>- **Supportability**: Showing current status plus decision notes provides useful information to identify and fix faults during use (e.g., why login is blocked, why an application was rejected), reducing back-and-forth and avoiding error-prone reconstructions from raw data.<br>- **Performance**: Because search is used repeatedly during operational triage, the system must return results within the defined response measure (e.g., p95 ≤ 2 seconds). Slow queries directly create admin backlogs and delay governance actions. | 5 | Should | Depends on user data model and indexing/search in admin UI/backend. |
| US-27 | **US-27 — Admin Suspend/Reactivate Partners (MVP)**: As a **System Administrator**, I want to **suspend and reactivate restaurant/shipper accounts**, so that **unsafe or non-compliant partners cannot operate**. **[QAs: Security, Reliability, Manageability, Supportability]** | - Given a partner account is approved, when I suspend it, then a suspension reason is required and the account status becomes `Suspended` (FR-4.8).<br>- Given an account is `Suspended`, then the system prevents the restaurant/shipper from receiving or processing new orders (FR-4.9).<br>- Given I reactivate an account, when I confirm, then status becomes `Active/Approved` and operations resume. | - **Security** (RBAC enforced for suspend/reactivate; actions audited; deny-by-default)<br>- **Reliability** (suspended partners blocked from receiving/processing orders 100% across intake/dispatch; enforcement effective within ≤ 1 minute)<br>- **Manageability** (suspend/reactivate completes within ≤ 1 minute; reason required and visible in admin view)<br>- **Supportability** (reason+actor+timestamp recorded 100% and queryable) | - **Security**: Suspending/reactivating partners is a high-impact control; the system must resist unintended activity by enforcing authorization and ensuring a suspended partner cannot receive/process new orders even if they attempt to bypass UI controls (FR-4.8/FR-4.9).<br>- **Reliability**: Status enforcement must be maintained over time and applied consistently across the order intake/dispatch path. Partial or inconsistent enforcement (some components blocking, others not) creates operational failures where restricted partners still operate.<br>- **Manageability**: This feature provides administrators a direct means to manage incidents and policy enforcement in production—containing risk quickly and restoring service via reactivation without destructive actions like deleting accounts.<br>- **Supportability**: Mandatory suspension reasons (and audit context) provide useful information to identify and resolve operational/compliance issues during use and support later investigations and partner communication. | 3 | Should | Depends on partner status model + enforcement in order intake/dispatch. |
| US-28 | **US-28 — Admin Monitor Orders & View Details (MVP)**: As a **System Administrator**, I want to **monitor orders and view order details**, so that **I can investigate issues and support operations**. **[QAs: Supportability, Manageability, Performance]** | - Given I open the order monitoring view, then I can see a list of orders and filter at minimum by status, time range, and restaurant (FR-4.10).<br>- Given I select an order, then I can view details including status history, assigned shipper (if any), and any cancellation reason (FR-4.11).<br>- Given monitoring data is displayed, then it is refreshed within a defined window (e.g., ≤ 60 seconds) to remain operationally useful. | - **Supportability** (order detail includes status history, assignments, cancellation reason; traceable via correlation ID)<br>- **Manageability** (filters by status/time/restaurant applied server-side; paging supported)<br>- **Performance** (refresh window ≤ 60 seconds; page load p95 ≤ 2 seconds for 1,000 active orders) | - **Supportability**: Order details, status history, assignments, and cancellation reasons are useful information to identify and fix faults during use—enabling admins to trace what happened and where a flow broke without manual database digging (FR-4.10/FR-4.11).<br>- **Manageability**: Filters (status/time/restaurant) are the administrator’s means to manage operations at runtime by narrowing attention to the subset that needs intervention and coordinating actions efficiently (FR-4.10).<br>- **Performance**: The monitoring UI must meet the defined refresh window (e.g., ≤ 60 seconds) and remain responsive under load; otherwise the view becomes stale and administrators cannot act in time to prevent cascading delays. | 5 | Should | Depends on order model and status history; related to US-19. |
| US-29 | **US-29 — Admin Cancel Order With Reason (MVP)**: As a **System Administrator**, I want to **cancel an order with a clear reason**, so that **exceptions can be handled safely and transparently**. **[QAs: Reliability, Security, Supportability]** | - Given I am authorized to intervene, when I cancel an order, then a cancellation reason is required and the system records actor identity and timestamp (FR-4.12).<br>- Given an order is canceled by an admin, then the system notifies affected parties (at minimum customer and restaurant; shipper if assigned) and the customer can see the cancellation reason (FR-2.4).<br>- Given the order is in `preparing`, `ready_for_pickup`, `picked_up`, or `delivering` state, when I attempt cancellation, then the system blocks it with a clear message; `delivered` orders also cannot be cancelled but admins can initiate a refund via a separate refund action (T-12, POST /orders/:id/refund). | - **Reliability** (blocks invalid states like `Delivered`; state+notifications consistent and idempotent under retries)<br>- **Security** (RBAC enforced; cancellation recorded with actor identity+timestamp; action audited)<br>- **Supportability** (reason code required; records queryable by order ID/time; correlation ID returned) | - **Reliability**: Admin cancellation must preserve correct order lifecycle behavior over time: prevent invalid transitions (e.g., blocking cancellation of a `Delivered` order) and ensure state changes and notifications remain consistent so exceptional handling doesn’t corrupt records or create conflicting outcomes.<br>- **Security**: Cancellation is a privileged action; the system must resist unintended activity by enforcing authorization and recording actor identity/timestamp to deter misuse and support accountability (FR-4.12).<br>- **Supportability**: A clear cancellation reason plus audit context provides useful information to identify and fix issues during use (complaints and disputes), reducing investigation effort and improving communication to affected parties. | 3 | Should | Depends on lifecycle constraints (US-8) and notification to parties (US-9). |
| US-30 | **US-30 — Admin Configure Commission Percentage & History (MVP)**: As a **System Administrator**, I want to **configure the commission percentage and keep a change history**, so that **commission reporting remains governed and auditable**. **[QAs: Manageability, Reliability, Supportability, Testability, Reusability]** | - Given I am authorized, when I update the commission percentage, then the system validates the value (e.g., 0–100%) and persists the new configuration (FR-4.13).<br>- Given the commission percentage changes, then the system records a history entry with effective date/time and the admin who performed the change (FR-4.14).<br>- Given reporting runs, then it uses the commission configuration applicable for the report time window or the per-order commission snapshot as defined by US-21. | - **Manageability** (commission % validated 0–100 and persisted; change takes effect within ≤ 1 minute)<br>- **Reliability** (each delivered order uses a single effective rate/snapshot; reports do not drift after config changes)<br>- **Supportability** (history stores old/new values + actor + timestamp; queryable in admin UI)<br>- **Testability** (automated tests cover boundary values and verify history entry creation per change)<br>- **Reusability** (versioned config-with-history component reusable for other operational settings) | - **Manageability**: Commission rate is operational configuration; the system must provide administrators means to manage and tune it safely via the dashboard (with validation) rather than code changes, enabling timely business adjustments (FR-4.13).<br>- **Reliability**: Reporting and commission calculations must remain correct over time. Using the applicable effective configuration (or per-order snapshot per US-21) and recording history prevents inconsistent results across periods and reduces revenue disputes (FR-4.14).<br>- **Supportability**: Change history (who/what/when/effective time) provides useful information to identify and fix faults during use—e.g., explaining why reports differ between windows or tracing unexpected commission shifts (FR-4.14).<br>- **Testability**: Range validation and history recording define clear criteria that can be tested (boundary values, persistence, and required fields for each history entry), enabling automated verification of both the rules and the audit trail.<br>- **Reusability**: Configuration-with-history should be implemented as a reusable platform capability (validated config values + versioned effective timestamps + immutable change log), not bespoke storage for one setting. The same mechanism can be reused for other operational knobs (service area boundaries, timeouts, thresholds) to minimize duplicated “config + audit” code and ensure all policy changes follow the same governance/audit pattern. | 5 | Could | Depends on commission model and snapshots (US-21). |
| US-37 | **US-37 — Admin Manage Platform Promotions**: As a **System Administrator**, I want to **create and govern platform-wide promotions**, so that **global campaigns remain controlled, auditable, and compatible with checkout**. **[QAs: Manageability, Security, Reliability, Supportability]** | - Given I am authorized, when I create or update a platform promotion, then global scope, validity, quota, coupon uniqueness, and discount rules are validated.<br>- Given a platform promotion is active, then checkout can apply it according to the same promotion contract used by restaurant promotions.<br>- Given a platform promotion is changed or disabled, then the decision is auditable and active checkout behavior follows the current rule. | - **Manageability** (global promotion rules are governed centrally)<br>- **Security** (admin-only mutation)<br>- **Reliability** (checkout uses one promotion contract)<br>- **Supportability** (changes are traceable) | Added by the traceability audit to close UC-24 as a direct admin story. | 5 | Could | Complements US-36 and checkout (US-7). |
| US-31 | **US-31 — Admin Reports Access & CSV Export (MVP)**: As a **System Administrator**, I want to **view and export platform reports**, so that **I can track operations and basic financial metrics**. **[QAs: Interoperability, Supportability, Manageability, Reusability]** | - Given I open the reports section, then I can access the logical reports defined for Release 1 (e.g., order volume, financial/commission summary, user registration & approval status) (FR-4.15).<br>- Given I apply filters (e.g., date range; restaurant for financial report), then the report results reflect the filters (SRS Reports section).<br>- Given I export a report, then the system provides a machine-readable export (e.g., CSV) with stable column headers suitable for offline reconciliation (FR-4.15). | - **Interoperability** (CSV export uses stable column headers and UTF-8; importable into Excel/Sheets without manual edits)<br>- **Supportability** (report outputs include filters used and are reproducible for the same inputs/time window)<br>- **Manageability** (server-side filters applied consistently; exports logged with actor+timestamp)<br>- **Reusability** (shared reporting query + CSV export pipeline reused across reports) | - **Interoperability**: CSV export with stable headers enables the platform to exchange and reuse report data in external tools (spreadsheets/BI/ETL) for reconciliation and analysis, rather than locking stakeholders into the admin UI (FR-4.15).<br>- **Supportability**: Reports provide useful information to identify and fix operational and financial issues during use (approval backlogs, cancellation spikes, commission anomalies) and reduce risky ad-hoc database access.<br>- **Manageability**: Filterable views and exports give administrators practical means to manage and tune the platform—monitoring KPIs, detecting anomalies, and taking corrective actions based on repeatable, consistent evidence (FR-4.15).<br>- **Reusability**: Reporting should use reusable building blocks (query filters, pagination, consistent aggregation utilities, and a CSV export pipeline with schema/versioned headers) so new reports can be added without copy/pasting bespoke export logic. Reuse reduces duplication across order/finance/user reports and ensures consistency in how “the same metric” is computed and exported, which is critical for trustworthy operations and reconciliation. | 8 | Could | Depends on stable data model + calculations (US-21, US-30). |
| US-32 | **US-32 — Admin Audit Log for Actions (MVP)**: As a **System Administrator**, I want **an immutable audit log for administrative actions**, so that **investigations and compliance needs are supported**. **[QAs: Security, Reliability, Supportability, Testability, Reusability]** | - Given an admin action occurs (e.g., approval/rejection, suspension/reactivation, order cancellation, commission config change), then the system records an immutable audit entry including admin identity, action type, target entity, timestamp, and before/after status when applicable (FR-4.16).<br>- Given I need to investigate an issue, when I query audit logs (read-only), then I can filter at minimum by time range and action type.<br>- Given audit logging is enabled, then failures to write an audit entry are treated as a high-severity error and the admin action is either blocked or retried according to policy (must be consistent across actions). | - **Security** (audit log access RBAC-protected; entries are append-only/immutable)<br>- **Reliability** (100% admin actions emit an audit entry; on write failure behavior is consistent (block or retry) and testable)<br>- **Supportability** (filter by time/action; entries include actor, target, timestamp, before/after, and correlation ID)<br>- **Testability** (required fields and failure handling validated in automated tests)<br>- **Reusability** (central audit subsystem reused by all privileged features) | - **Security**: An immutable audit log resists unintended activities by creating accountability for privileged actions and supporting governance; recording actor, action, target, and before/after state helps detect and deter misuse (FR-4.16).<br>- **Reliability**: The audit mechanism must be maintained over time and operate dependably: every admin action must generate an entry, and failures to write must follow a consistent block-or-retry policy so evidence is not silently lost and behavior is predictable across actions.<br>- **Supportability**: Queryable audit records provide useful information to identify and fix faults during use—supporting incident response, dispute resolution, and “who changed what and when” investigations via filtering and immutable history.<br>- **Testability**: A stable schema and deterministic logging/failure-handling rules define clear criteria for automated tests (required fields, before/after capture, and consistent behavior when the audit sink fails).<br>- **Reusability**: Audit logging should be a reusable, centralized subsystem (shared schema, append-only storage, and standard “event envelope” fields) used by all privileged features rather than each feature inventing its own audit format. Reuse minimizes duplication, enables consistent querying/correlation across actions, and makes it feasible to extend auditing to new modules or even other applications without redesigning logging every time. | 8 | Could | Cross-cutting across admin actions; implement after core flows stabilize (or earlier if required). |

---

## Platform / Internal

| ID | User Story | Acceptance Criteria | Quality Attributes | Justification | SP | Priority (MoSCoW) | Key Dependencies / Notes |
|---|---|---|---|---|---:|---|---|
| US-8 | **US-8 — Order Lifecycle Integrity**: As a **Platform**, I want **orders to progress through strict statuses**, so that **tracking is accurate and operations stay consistent**. **[QAs: Reliability, Conceptual Integrity, Maintainability, Testability, Reusability]** | - Given a COD order is `pending`, when a restaurant confirms it (T-01), then status becomes `confirmed`; for a VNPay order already in `paid` state, the restaurant confirms via T-04 to advance to `confirmed`.<br>- Given a VNPay order is `pending` and the backend receives a valid payment IPN callback (T-02, system-triggered), then status becomes `paid`.<br>- Given an order is `confirmed`, when preparation starts (T-06), then status becomes `preparing`.<br>- Given an order is `preparing`, when it is ready for pickup (T-08), then status becomes `ready_for_pickup`.<br>- Given an order is `ready_for_pickup`, when a shipper self-assigns and picks it up (T-09), then status becomes `picked_up` and the shipper is recorded on the order.<br>- Given an order is `picked_up`, when the shipper marks it en route (T-10), then status becomes `delivering`.<br>- Given an order is `delivering`, when delivery is confirmed (T-11), then status becomes `delivered`.<br>- Given an admin initiates a post-delivery refund (T-12, admin only, requires reason), then status transitions from `delivered` to `refunded`.<br>- Given a user attempts an out-of-sequence transition, then the system rejects it with a specific error (BR-7).<br>- Given an order in `pending` or `paid` state exceeds the configured `RESTAURANT_ACCEPT_TIMEOUT_SECONDS` without restaurant confirmation, then the system auto-cancels it (status `cancelled`) with a system-generated reason note, and the customer is notified. | - **Reliability** (rejects all out-of-sequence transitions 100%; idempotent under retries; no state corruption in regression suite)<br>- **Conceptual Integrity** (single status model shared across services/UI; contract tests assert allowed status values)<br>- **Maintainability** (adding a status requires changes in ≤ 2 modules (lifecycle + mapping); verified via PR checklist)<br>- **Testability** (automated transition-matrix tests cover 100% allowed/forbidden edges per BR-7)<br>- **Reusability** (shared lifecycle module reused by restaurant/shipper/admin transitions; no duplicated rules) | - **Reliability**: The order lifecycle is a system-wide state machine touched by restaurant actions, shipper updates, tracking UI, notifications, and reporting. If even one invalid/out-of-sequence transition slips through (e.g., `Delivered` before `Picked Up`, or “re-accepting” a canceled order), different components will observe contradictory states and start making wrong decisions (wrong ETA, wrong dispatch, wrong analytics). Therefore, transitions must be strictly validated and invalid attempts must be rejected deterministically with a specific error (BR-7) to prevent state corruption that later becomes system-wide operational failures.<br>- **Conceptual Integrity**: “Order status” must have one coherent meaning across the whole design: API contracts, database state, UI labels, and event/notification semantics. If modules interpret statuses differently (e.g., one service treats `Ready for Pickup` as “shipper assigned” while another treats it as “food ready”), the architecture loses its overall consistency and behaviors become unpredictable. A single lifecycle definition preserves a shared mental model, keeps cross-module coordination sane, and prevents subtle inconsistencies that are expensive to debug and explain.<br>- **Maintainability**: Lifecycle rules change (new statuses, cancellation paths, exceptional handling). Centralizing transition rules in one lifecycle module/state-machine boundary avoids scattering “if status == …” conditionals across services. That reduces ripple effects when requirements evolve, lowers regression risk in multiple code paths, and makes it feasible to extend the state machine without accidentally creating dead-ends or illegal shortcuts.<br>- **Testability**: A finite status graph enables complete, objective test coverage: for each state, all allowed transitions must succeed and all forbidden transitions must fail with the expected error (BR-7). This supports automated transition-matrix tests and regression suites that catch broken edge cases (retries, duplicate events, late updates) before they reach production and create inconsistent order histories.<br>- **Reusability**: The lifecycle/transition-validation logic should be a reusable component used by every actor that can change order state (restaurant updates, shipper pickup/delivery confirmation, and admin interventions). Reusing one state-machine module prevents each service/UI from re-encoding transition rules differently, avoids duplicated “status if/else” logic, and keeps the platform’s state semantics consistent as new statuses or order types are introduced. | 8 | Must | Foundation for checkout/fulfillment/tracking; implement early. |
| US-20 | **US-20 — Geographic Scope Restriction (MVP)**: As a **Platform**, I want to **restrict ordering to a single service area**, so that **MVP operations remain controlled and feasible**. **[QAs: Conceptual Integrity, Flexibility, Reliability, Reusability]** | - Given a customer address is outside the configured service area, when they attempt to place an order, then the system blocks order submission (BR-6).<br>- Given the service area definition changes, when configuration is updated, then the new boundary takes effect without code changes (if configuration-based).<br>- Given the same address is checked repeatedly, results are consistent across sessions. | - **Conceptual Integrity** (service-area decision consistent across discovery/address/checkout; no contradictory “serviceable” results in regression set)<br>- **Flexibility** (service area is config-driven (polygon/radius); changes take effect within ≤ 5 minutes without code changes)<br>- **Reliability** (same address+config yields same decision 100% in tests; deterministic evaluation)<br>- **Reusability** (geo-policy module reused by proximity search, address validation, and checkout blocking) | - **Conceptual Integrity**: The “single service area” constraint (BR-6) is not just a validation check—it defines the platform’s operating model for MVP. The rule must be enforced consistently across discovery, address validation, and checkout so the system has one shared interpretation of “serviceable.” If different modules apply different rules (or apply them at different times), users can progress deep into the funnel only to be blocked later, and operations will face hard-to-explain edge cases (orders created then canceled due to geography), which erodes platform consistency and trust.<br>- **Flexibility**: Service boundaries are operational policy and will change as the business learns and expands (tighten radius, redefine polygon, shift to administrative zones). Making the boundary configurable allows the system to adapt to new environments and business rules without refactoring core ordering logic or redeploying frequently, reducing time-to-change and avoiding “code as policy” that becomes brittle and slow to update.<br>- **Reliability**: The same address evaluated under the same configuration must yield the same accept/reject outcome over time and across sessions; otherwise the platform behaves unpredictably. Intermittent boundary decisions create concrete failure modes: accepting orders that cannot be fulfilled (leading to cancellations/refunds and support load) or rejecting valid orders (direct revenue loss). Deterministic, repeatable checks are therefore required to keep ordering decisions stable and operationally safe.<br>- **Reusability**: “Is this address inside the allowed service boundary?” should be implemented as a reusable policy/enforcement module (boundary representation + evaluation + standardized decision output) reused by checkout blocking, address validation, and any future geo-based rules. Reuse prevents teams from duplicating geometry/radius logic in multiple places and eliminates the common failure mode where two modules disagree on whether the same address is serviceable. | 3 | Must | Enforce early to constrain MVP operational scope and avoid unsupported orders. |
| US-21 | **US-21 — Commission Calculation (Internal)**: As a **Platform Operator**, I want to **calculate expected commission per restaurant based on GMV**, so that **financial reporting is consistent**. **[QAs: Reliability, Flexibility, Testability, Maintainability, Reusability]** | - Given an order is marked `Delivered`, when the final paid amount is recorded (COD collected by shipper, or VNPay-paid amount confirmed by the gateway), then GMV and commission are calculated using the configured fixed percentage (BR-5).<br>- Given the commission rate changes, when configuration is updated, then the system uses the new rate only for orders delivered after the change, and each delivered order stores a snapshot of the commission rate used so historical records remain unchanged.<br>- Given rounding rules, then calculations follow consistent rounding to currency precision. | - **Reliability** (computed deterministically at `Delivered`; per-order snapshot stored; rounding consistent to currency precision)<br>- **Flexibility** (rate changes apply only after effective time; snapshot preserves historical values)<br>- **Testability** (unit tests cover rounding and rate-change boundaries; outputs asserted exactly)<br>- **Maintainability** (commission logic isolated so changes touch ≤ 2 modules: calculator + config adapter)<br>- **Reusability** (calculator module reused by reports/exports/dashboards to ensure consistent numbers) | - **Reliability**: Commission is a financial control point: it impacts platform revenue recognition, restaurant settlement expectations, and audit/reconciliation. If GMV or commission is computed inconsistently (especially around `Delivered` events, payment confirmation timing, and rounding), “small” errors accumulate into material discrepancies across many orders—triggering disputes, manual corrections, and loss of trust in reports. Therefore, the calculation must be deterministic at delivery-time, follow a single rounding policy to currency precision, and preserve the per-order result so historical reporting does not drift (BR-5).<br>- **Flexibility**: Commission percentage is a business rule that will change (promotions, new partner terms, operational adjustments). Supporting configuration changes—while applying them only to orders delivered after the effective change and snapshotting the applied rate per order—lets the platform adapt to evolving policies without rewriting history or reprocessing past data, which would otherwise cause inconsistencies and costly backfills.<br>- **Testability**: Because the inputs and rules are explicit (final paid amount, configured percentage, rounding), the feature can be verified with precise expected outputs. Automated tests can cover boundary scenarios (rate changes at time boundaries, rounding edge values, zero/discount cases where applicable) and assert that the stored snapshot exactly matches the rate used, preventing silent regressions that would only be discovered during finance reconciliation.<br>- **Maintainability**: Commission logic is likely to evolve (new rounding rules, tiered rates, per-restaurant overrides, fee exclusions). Encapsulating the computation and snapshot responsibility reduces coupling to reporting and order services, making future changes safer and localized. This lowers the risk that a “simple change” accidentally recalculates historical commissions or breaks downstream reports and dashboards.<br>- **Reusability**: Commission/GMV computation should be implemented as a reusable calculation module (pure function + shared rounding policy + consistent inputs) consumed by reporting, dashboards, CSV exports, and any settlement/invoicing features. Reuse avoids copy/pasting financial formulas across multiple code paths (a common source of subtle discrepancies) and ensures every consumer produces the same numbers for the same order snapshot. | 5 | Could | Depends on delivered orders (US-17) and final paid amount recording for COD/VNPay. |

---

## Notes (configuration-based measurability)

Several acceptance criteria use **configuration-based thresholds** (timeouts, SLAs, “stuck” detection). This keeps them measurable/testable while letting you tune values during MVP.

You can finalize and document these configuration values for your project:
- Notification/real-time SLAs per role and network baseline.
- Restaurant accept timeout and per-status “stuck” thresholds.
- Service area boundary representation (polygon, radius, administrative region).

---

## Traceability Matrix

### Lean Trace Review Scope

This trace review keeps only evidence-based links. One valid downstream trace is sufficient; additional mappings are retained only when the source document, SRS use case, ASR driver, ADD element, ADR, SAD view, or implementation module directly owns or realizes the relationship.

Locked content was not changed. The review modifies only trace relationships, trace notes, architecture references, and orphan-review wording.

### Trace Review Summary

| Review Area | Issue Found | Correction Applied |
|---|---|---|
| Trace closure rule | Earlier wording encouraged coverage-driven closure instead of evidence-based mapping | Replaced with lean trace rule |
| US to UC mapping | Several rows included secondary notification, observer, refund, or producer-side dependencies as direct UCs | Removed only clearly secondary UC links |
| UC to ASR mapping | Some rows mixed feature similarity with architectural-driver dependency | Kept ASRs that directly drive the UC behavior |
| ASR to ADR mapping | Several ADR lists were broad architecture associations rather than direct decisions | Pruned ADR lists to decisions evidenced by ASR/ADR/SAD sources |
| Code to requirement mapping | Some module rows used broad ranges or all-state coverage language | Replaced with explicit, lean module ownership mappings |
| ADR to SAD mapping | ADR-to-view coverage was implicit | Added a compact ADR-to-SAD view matrix |

### Matrix 1: User Story ↔ UC

| User Story ID | Description | UC IDs | Status | Notes |
|---|---|---|---|---|
| US-1 | Customer registration/login | UC-1; UC-DOM-01 | OK | Auth foundation for customer identity and role-based channels. |
| US-2 | Unified restaurant and food discovery | UC-2, UC-3; UC-DOM-02 | OK | Covers search and restaurant detail navigation. |
| US-4 | View restaurant/menu availability | UC-3; UC-DOM-02 | OK | Customer-facing availability display. |
| US-5 | Single-restaurant cart constraint | UC-4, UC-5; UC-DOM-03 | OK | BR-2 invariant enforced during cart add/update flows. |
| US-6 | Delivery address within radius | UC-6, UC-8; UC-DOM-03 | OK | Address validation and checkout blocking. |
| US-7 | Checkout with COD or VNPay | UC-8, UC-9; UC-DOM-03, UC-DOM-04 | OK | Checkout and VNPay initiation/IPN behavior. |
| US-8 | Order lifecycle integrity | UC-8, UC-14, UC-15, UC-18, UC-19, UC-25; UC-DOM-05, UC-DOM-07 | OK | Direct lifecycle creation, transition, delivery, and refund-state ownership. |
| US-9 | Real-time order status updates | UC-20, UC-26; UC-DOM-05, UC-DOM-08, UC-DOM-11 | OK | Notification and tracking surfaces. |
| US-10 | Restaurant onboarding approval | UC-11, UC-27; UC-DOM-06, UC-DOM-10 | OK | Restaurant-side request plus admin approval. |
| US-11 | Restaurant menu management | UC-12; UC-DOM-06 | OK | Menu CRUD and catalog mutation. |
| US-12 | Restaurant availability control | UC-13; UC-DOM-06 | OK | Producer-side Sold Out/Closed controls. |
| US-13 | Accept/reject incoming orders | UC-14, UC-26; UC-DOM-06, UC-DOM-08 | OK | Restaurant order intake and alerting. |
| US-14 | Shipper onboarding approval | UC-16, UC-28; UC-DOM-07, UC-DOM-10 | OK | Shipper-side request plus admin approval. |
| US-15 | Shipper availability toggle | UC-17; UC-DOM-07 | OK | Delivery eligibility state. |
| US-16 | Shipper accept and pick up order | UC-18; UC-DOM-07 | OK | Atomic assignment and pickup. |
| US-17 | Shipper confirm delivery | UC-19; UC-DOM-07 | OK | Delivery completion and closure. |
| US-18 | Admin approve/reject partners | UC-27, UC-28; UC-DOM-10 | OK | Restaurant and shipper partner gate. |
| US-19 | Admin monitor order/platform health | UC-30, UC-34; UC-DOM-10, UC-DOM-12 | OK | Operational health and dashboard overview. |
| US-20 | Geographic scope restriction | UC-7, UC-8; UC-DOM-03, UC-DOM-06 | OK | Service-area configuration and checkout enforcement. |
| US-21 | Commission calculation | UC-33; UC-DOM-12 | OK | Financial calculation feeding operational reports. |
| US-22 | Shopping cart management | UC-4, UC-5; UC-DOM-03 | OK | Cart add/update/remove and totals. |
| US-23 | Restaurant update preparation status | UC-15; UC-DOM-06 | OK | Restaurant preparation transitions. |
| US-24 | Restaurant cancel order with reason | UC-14, UC-21; UC-DOM-06 | OK | Restaurant cancellation/rejection path; customer path is US-35. |
| US-25 | Admin dashboard access and RBAC | UC-34, UC-35; UC-DOM-10 | OK | Dashboard access and permissions. |
| US-26 | Admin search user accounts | UC-31; UC-DOM-10 | OK | User search and account governance. |
| US-27 | Admin suspend/reactivate partners | UC-29; UC-DOM-10 | OK | Partner operational restriction. |
| US-28 | Admin monitor orders and view details | UC-30, UC-34; UC-DOM-10, UC-DOM-12 | OK | Read-only order investigation and dashboard detail. |
| US-29 | Admin cancel order with reason | UC-32; UC-DOM-10 | OK | Admin exception path for cancellation/refund override. |
| US-30 | Admin configure commission percentage/history | UC-33; UC-DOM-10, UC-DOM-12 | OK | Financial configuration feeding reports. |
| US-31 | Admin reports access and CSV export | UC-33; UC-DOM-12 | OK | Reporting and export. |
| US-32 | Admin audit log for actions | UC-DOM-10 | OK | Administrative governance domain owns audit-log inspection. |
| US-33 | Review and rating | UC-22; UC-DOM-09 | OK | Rating and review submission. |
| US-34 | Customer view order history | UC-10; UC-DOM-05 | OK | Direct order-history coverage. |
| US-35 | Customer cancel order | UC-21; UC-DOM-05 | OK | Direct customer cancellation trace. |
| US-36 | Restaurant manage promotions | UC-23; UC-DOM-06 | OK | Direct restaurant-promotion trace. |
| US-37 | Admin manage platform promotions | UC-24; UC-DOM-10 | OK | Direct platform-promotion trace. |

### Matrix 2: UC ↔ SRS ↔ ASR ↔ QA

| UC | SRS Section | ASR | QA Scenario | Status |
|---|---|---|---|---|
| UC-1 User Authentication | SRS UC-1; SD-1; UC-DOM-01 | AD-3, AD-11 | QA-S-02, QA-A-01, QA-S-06, QA-U-01 | OK |
| UC-2 Discover Restaurants & Food | SRS UC-2; SD-2; UC-DOM-02 | AD-3, AD-11 | QA-P-01, QA-SC-01, QA-S-05, QA-U-02 | OK |
| UC-3 View Restaurant Details | SRS UC-3; SD-3; UC-DOM-02 | AD-3 | QA-P-01 | OK |
| UC-4 Add Item to Cart | SRS UC-4; SD-4; UC-DOM-03 | AD-6 | QA-R-04, QA-SC-02, QA-S-02 | OK |
| UC-5 Manage Shopping Cart | SRS UC-5; SD-5; UC-DOM-03 | AD-6 | QA-R-04, QA-SC-02, QA-S-02 | OK |
| UC-6 Save & Manage Delivery Addresses | SRS UC-6; SD-6; UC-DOM-03 | AD-7 | QA-R-04 | OK |
| UC-7 Manage Delivery Zones | SRS UC-7; SD-7; UC-DOM-06 | AD-7, AD-3 | QA-MA-01, QA-CI-02 | OK |
| UC-8 Place Order | SRS UC-8; SD-8; UC-DOM-03 | AD-1, AD-6, AD-7, AD-12 | QA-R-01, QA-P-03, QA-R-04, QA-R-08, QA-SC-02, QA-T-01, QA-CI-01 | OK |
| UC-9 Make Online Payment (VNPay) | SRS UC-9; SD-9; UC-DOM-04 | AD-2 | QA-S-01, QA-R-02, QA-R-06, QA-I-01 | OK |
| UC-10 View Order History | SRS UC-10; SD-10; UC-DOM-05 | AD-10 | QA-SUP-01, QA-P-01 | OK |
| UC-11 Restaurant Registration & Profile Management | SRS UC-11; SD-11; UC-DOM-06 | AD-8 | QA-S-03, QA-SUP-02 | OK |
| UC-12 Manage Menu Catalog | SRS UC-12; SD-12; UC-DOM-06 | AD-3 | QA-P-04, QA-MA-01, QA-I-03, QA-S-05 | OK |
| UC-13 Toggle Item & Restaurant Availability | SRS UC-13; SD-13; UC-DOM-06 | AD-3 | QA-P-04, QA-MA-01, QA-SUP-02, QA-CI-02 | OK |
| UC-14 Accept or Reject Order | SRS UC-14; SD-14; UC-DOM-06 | AD-4, AD-5, AD-10, AD-12 | QA-P-02, QA-R-03, QA-R-07, QA-R-08, QA-SUP-01 | OK |
| UC-15 Prepare Order for Pickup | SRS UC-15; SD-15; UC-DOM-06 | AD-4, AD-5, AD-10 | QA-P-02, QA-R-03, QA-FL-02, QA-SUP-01, QA-CI-01 | OK |
| UC-16 Shipper Registration | SRS UC-16; SD-16; UC-DOM-07 | AD-8 | QA-S-03, QA-SUP-02 | OK |
| UC-17 Manage Shipper Availability | SRS UC-17; SD-17; UC-DOM-07 | AD-8 | QA-S-03 | OK |
| UC-18 Accept Delivery Assignment | SRS UC-18; SD-18; UC-DOM-07 | AD-4, AD-5, AD-10 | QA-P-02, QA-R-03, QA-R-05, QA-SUP-01, QA-CI-01 | OK |
| UC-19 Deliver Order | SRS UC-19; SD-19; UC-DOM-07 | AD-4, AD-5, AD-10 | QA-P-02, QA-R-03, QA-SUP-01, QA-CI-01 | OK |
| UC-20 Track Order Status | SRS UC-20; SD-20; UC-DOM-05, UC-DOM-11 | AD-4, AD-9 | QA-P-02, QA-A-02, QA-I-02, QA-CI-01 | OK |
| UC-21 Cancel Order | SRS UC-21; SD-21; UC-DOM-05 | AD-5, AD-10, AD-12 | QA-R-03, QA-R-08, QA-FL-02, QA-SUP-01, QA-CI-01 | OK |
| UC-22 Submit Rating & Review | SRS UC-22; SD-22; UC-DOM-09 | AD-5, AD-10 | QA-S-02, QA-S-05, QA-SUP-01 | OK |
| UC-23 Manage Restaurant Promotions | SRS UC-23; SD-23; UC-DOM-06 | AD-3, AD-12 | QA-R-08, QA-MA-01, QA-S-03, QA-FL-01 | OK |
| UC-24 Manage Platform Promotions | SRS UC-24; SD-24; UC-DOM-10 | AD-10, AD-12 | QA-S-03, QA-R-08, QA-SUP-02 | OK |
| UC-25 Process Payment Refund | SRS UC-25; SD-25; UC-DOM-04 | AD-2, AD-12 | QA-R-08, QA-I-01, QA-MA-01, QA-SUP-02, QA-CI-02 | OK |
| UC-26 Manage Real-Time Notifications | SRS UC-26; SD-26; UC-DOM-08 | AD-4, AD-9 | QA-I-02, QA-A-02, QA-A-03, QA-P-02, QA-FL-03 | OK |
| UC-27 Approve or Reject Restaurant Applications | SRS UC-27; SD-27; UC-DOM-10 | AD-8, AD-10 | QA-S-03, QA-MA-01, QA-SUP-02, QA-CI-02 | OK |
| UC-28 Approve or Reject Shipper Applications | SRS UC-28; SD-28; UC-DOM-10 | AD-8, AD-10 | QA-S-03, QA-SUP-02 | OK |
| UC-29 Suspend or Reactivate Partner Accounts | SRS UC-29; SD-29; UC-DOM-10 | AD-8, AD-10 | QA-S-03, QA-SUP-02, QA-CI-01 | OK |
| UC-30 Monitor Orders and Platform Health | SRS UC-30; SD-30; UC-DOM-10, UC-DOM-12 | AD-10 | QA-S-03, QA-SUP-03, QA-P-01 | OK |
| UC-31 Search and Manage User Accounts | SRS UC-31; SD-31; UC-DOM-10 | AD-10 | QA-S-03, QA-SUP-02, QA-P-01 | OK |
| UC-32 Administrative Order Cancellation & Refund | SRS UC-32; SD-32; UC-DOM-10 | AD-5, AD-10, AD-12 | QA-R-03, QA-R-08, QA-FL-02, QA-SUP-01, QA-CI-01 | OK |
| UC-33 View and Export Operational Reports | SRS UC-33; SD-33; UC-DOM-12 | AD-10 | QA-S-03, QA-SUP-03, QA-I-02 | OK |
| UC-34 View Dashboard & Platform Overview | SRS UC-34; SD-34; UC-DOM-10, UC-DOM-12 | AD-10 | QA-SUP-03, QA-P-01 | OK |
| UC-35 Manage Admin Roles & Permissions | SRS UC-35; SD-35; UC-DOM-10 | AD-8, AD-10 | QA-S-03, QA-SUP-02, QA-CI-01 | OK |

### Matrix 3: ASR ↔ ADD ↔ ADR

| ASR | QA Scenario | ADD Element | ADR | Status |
|---|---|---|---|---|
| AD-1 Exactly-once order creation | QA-R-01, QA-P-03 | ADD checkout latency, idempotency, Redis + DB unique backstop, Ordering command handler | ADR-004, ADR-006, ADR-008 | OK |
| AD-2 Online payment integrity | QA-S-01, QA-R-02, QA-I-01 | ADD VNPay callback integrity, IPN idempotency, Payment BC, VNPay adapter | ADR-007, ADR-008 | OK |
| AD-3 BC decoupling under one deployable | QA-MA-01, QA-CI-02, QA-SC-01 | ADD Logical View, Implementation View, Domain Events Hub, ACL snapshots, shared ports | ADR-001, ADR-002, ADR-005 | OK |
| AD-4 Real-time status visibility | QA-P-02, QA-A-02, QA-I-02 | ADD Notification Gateway, Socket.IO, Redis presence, notification inbox | ADR-004, ADR-006 | OK |
| AD-5 Order state-machine integrity | QA-R-03, QA-CI-01, QA-T-01 | ADD Order Lifecycle, transitions map, optimistic locking, order status logs | ADR-002, ADR-008 | OK |
| AD-6 Single-restaurant cart | QA-R-04, QA-SC-02 | ADD Redis cart repository, cart service, checkout validation | ADR-006 | OK |
| AD-7 Delivery radius constraint | QA-R-04, QA-MA-01 | ADD Geo service, delivery-zone snapshots, Ordering ACL repositories | ADR-005 | OK |
| AD-8 Partner approval integrity | QA-S-03, QA-SUP-02 | ADD Auth/RBAC, Restaurant Catalog approval, Admin/Governance context | ADR-002 | OK |
| AD-9 Optional notification degradation | QA-A-03, QA-I-02, QA-FL-03 | ADD Notification channel adapters, Noop email, Stub push, durable inbox | ADR-007 | OK |
| AD-10 Privileged-action auditability | QA-SUP-01, QA-SUP-02, QA-SUP-03 | ADD order status logs, payment transactions, notification delivery logs, admin governance | ADR-003, ADR-008 | OK |
| AD-11 Public endpoint abuse control | QA-S-06 | ADD Security constraints, Redis rate-limit bucket target | ADR-006 | OK |
| AD-12 Post-commit compensation reliability | QA-R-08, QA-R-06 | ADD refund handlers, payment timeout, promotion rollback, event failure handling | ADR-004, ADR-007, ADR-008 | OK |

### Matrix 4: Code ↔ Requirement ↔ Architecture

| Module | User Story | UC | ASR | ADD | ADR | Status |
|---|---|---|---|---|---|---|
| `apps/api/src/module/auth` + `src/lib/auth.ts` | US-1, US-25 | UC-1, UC-35 | AD-8, AD-11 | Auth BC, Better Auth, RBAC/security constraints | ADR-002, ADR-008 | OK |
| `restaurant-catalog` | US-2, US-4, US-10, US-11, US-12 | UC-2, UC-3, UC-7, UC-11, UC-12, UC-13, UC-27 | AD-3, AD-7, AD-8 | Restaurant Catalog BC, Catalog events, search, delivery zones | ADR-002, ADR-004, ADR-005, ADR-008 | OK |
| `image` | US-11 | UC-12 | AD-3 | Image BC, Cloudinary provider, external image storage | ADR-002, ADR-007, ADR-008 | OK |
| `ordering/cart` | US-5, US-22 | UC-4, UC-5 | AD-6 | Redis cart, single-restaurant invariant | ADR-003, ADR-006, ADR-008 | OK |
| `ordering/order` checkout | US-6, US-7, US-8, US-20 | UC-8, UC-9 | AD-1, AD-6, AD-7 | PlaceOrderHandler, ports, ACL snapshot reads, transaction | ADR-004, ADR-005, ADR-006, ADR-008 | OK |
| `ordering/order-lifecycle` | US-8, US-13, US-16, US-17, US-23, US-24, US-35 | UC-14, UC-15, UC-18, UC-19, UC-21 | AD-4, AD-5, AD-10, AD-12 | State machine, status logs, timeout task | ADR-002, ADR-004, ADR-008 | OK |
| `ordering/acl` | US-6, US-7, US-12, US-20 | UC-7, UC-8, UC-13 | AD-3, AD-7 | Ordering ACL snapshots and projectors | ADR-005, ADR-008 | OK |
| `ordering/order-history` | US-34 | UC-10 | AD-10 | Order history and status timeline | ADR-002, ADR-003, ADR-008 | OK |
| `payment` | US-7, US-29, US-35 | UC-9, UC-25, UC-32 | AD-2, AD-12 | VNPay adapter, IPN handler, timeout/refund handlers | ADR-002, ADR-004, ADR-007, ADR-008 | OK |
| `promotion` | US-7, US-36, US-37 | UC-23, UC-24 | AD-3, AD-12 | Promotion BC, pricing engine, reservation/rollback | ADR-002, ADR-007, ADR-008 | OK |
| `notification` | US-9, US-13, US-23, US-24, US-35 | UC-20, UC-26 | AD-4, AD-9, AD-10 | Notification BC, inbox, WebSocket, FCM/email adapters, Redis presence | ADR-002, ADR-004, ADR-006, ADR-007, ADR-008 | OK |
| `review` module | US-33 | UC-22 | AD-5, AD-10 | Review & Rating BC in ADD Logical View | ADR-001, ADR-002 | Trace validated; requirement-to-architecture coverage recorded |
| `admin` module | US-18, US-19, US-25, US-26, US-27, US-28, US-29, US-30, US-31, US-32, US-37 | UC-27, UC-28, UC-29, UC-30, UC-31, UC-32, UC-33, UC-34, UC-35 | AD-8, AD-10 | Admin/Governance context in ADD Logical View | ADR-001, ADR-002, ADR-003, ADR-008 | Trace validated; Admin/Governance coverage recorded |
| `shipper/delivery` module | US-14, US-15, US-16, US-17 | UC-16, UC-17, UC-18, UC-19 | AD-4, AD-5, AD-8 | Delivery Operations in ADD/SRS; current lifecycle stores `shipperId` | ADR-001, ADR-002, ADR-003, ADR-008 | Trace validated; Delivery Operations coverage recorded |
| `apps/mobile` | US-1, US-2, US-5, US-6, US-7, US-9, US-22, US-33, US-34, US-35 | UC-1, UC-2, UC-3, UC-4, UC-5, UC-6, UC-8, UC-9, UC-10, UC-20, UC-21, UC-22, UC-26 | AD-1, AD-4, AD-6, AD-7, AD-9 | Mobile client, notification socket, checkout, cart, orders | ADR-001, ADR-006, ADR-007 | OK |
| `apps/web` | US-10, US-11, US-12, US-13, US-23, US-24, US-36 | UC-11, UC-12, UC-13, UC-14, UC-15, UC-21, UC-23 | AD-3, AD-4, AD-5, AD-8 | Restaurant dashboard client for menu/orders/auth/image | ADR-001, ADR-002, ADR-007 | OK |
| PostgreSQL + Drizzle | US-1, US-7, US-8, US-21, US-32 | UC-1, UC-8, UC-9, UC-14, UC-25, UC-32, UC-33 | AD-1, AD-2, AD-5, AD-10, AD-12 | Data View, DB_CONNECTION, module-owned schemas | ADR-003, ADR-008 | OK |
| Redis / Valkey | US-5, US-7, US-9, US-22 | UC-4, UC-5, UC-8, UC-20, UC-26 | AD-1, AD-4, AD-6, AD-11 | Runtime state, cart, locks, idempotency, presence, rate-limit buckets | ADR-006 | OK |

### Matrix 5: ADR ↔ SAD View

| ADR | Direct SAD View Evidence | Status |
|---|---|---|
| ADR-001 Modular Monolith Architecture | Logical View; Implementation View; Deployment View; Development / Process View | OK |
| ADR-002 Bounded-Context Separation | Logical View; Implementation View; Data View | OK |
| ADR-003 Database per BC Ownership | Data View; Relations Among Views | OK |
| ADR-004 In-process EventBus Communication | Logical View; Runtime View | OK |
| ADR-005 ACL Snapshot Pattern | Logical View; Data View; Runtime View | OK |
| ADR-006 Redis Runtime Layer | Data View; Runtime View; Deployment View | OK |
| ADR-007 Ports and Adapters Integration Pattern | Implementation View; Runtime View; Deployment View | OK |
| ADR-008 Drizzle Type-safe Persistence Layer | Implementation View; Data View; Development / Process View | OK |

### Matrix 6: Orphan Audit Report

| Item Type | Finding | Repair Applied | Final Status |
|---|---|---|---|
| User Story without UC | None after lean trace review | Matrix 1 maps every US to at least one direct UC or UC-DOM entry | ZERO ORPHAN |
| UC without User Story | None after lean trace review | UC-1 through UC-35 retain at least one direct story/domain anchor | ZERO ORPHAN |
| UC without SRS | None | SRS UC-1 through UC-35 and SD-1 through SD-35 exist | ZERO ORPHAN |
| ASR without UC | None after lean trace review | Matrix 2 connects UC rows to AD-1 through AD-12 where directly driven | ZERO ORPHAN |
| QA without ASR | None after lean trace review | Matrix 2 and Matrix 3 preserve QA-to-ASR links with direct driver evidence | ZERO ORPHAN |
| QA without ADD | None after lean trace review | Matrix 3 maps QA groups to direct ADD elements | ZERO ORPHAN |
| ADD element without driver | None after lean trace review | Matrix 3 maps ADD elements to AD drivers and ADRs without coverage-only expansion | ZERO ORPHAN |
| ADR without SAD view | None after lean trace review | Matrix 5 maps ADR-001 through ADR-008 to direct SAD views | ZERO ORPHAN |
| Code without requirement | No major module remains untraced | Matrix 4 maps backend modules, clients, PostgreSQL/Drizzle, and Redis/Valkey to explicit requirements | ZERO ORPHAN |
| Forced secondary trace | Secondary notification, observer, producer-side, and broad infrastructure links were present | Removed when not direct evidence | RESOLVED |
| Architecture element without rationale | None after lean trace review | Matrix 3 and Matrix 5 bind elements to ASR drivers, ADRs, and SAD views | ZERO ORPHAN |

### Final Validation Summary

| Check | Result |
|---|---|
| User Stories traced | PASS |
| SRS UCs traced | PASS |
| UC-DOM entries traced | PASS |
| ASR drivers traced | PASS |
| QA scenario IDs traced | PASS |
| ADD elements traced | PASS |
| ADR decisions justified | PASS |
| ADR decisions mapped to SAD views | PASS |
| Major code modules traced with lean ownership | PASS |
| Remaining trace orphan count | 0 |
| Artificial / forced trace count | 0 |

## Submission Readiness Checklist

| Area | Result |
|---|---|
| User story content preserved | PASS |
| Acceptance criteria content preserved | PASS |
| QA mappings preserved | PASS |
| Story IDs preserved | PASS |
| Trace relations reviewed using evidence-based lean mapping | PASS |
| Document control added | PASS |
| Table of contents added | PASS |
| References consolidated | PASS |
| DOCX-friendly section organization | PASS |

## Glossary and Acronyms

| Term | Meaning |
|---|---|
| AC | Acceptance Criteria |
| ADR | Architecture Decision Record |
| ASR | Architecturally Significant Requirement |
| BR | Business Rule |
| COD | Cash on Delivery |
| DOCX | Microsoft Word document format |
| QA | Quality Attribute |
| SRS | Software Requirements Specification |
| UC | Use Case |
| VNPay | Online payment gateway used by the platform |

## References

- [Business_Rules.md](Business_Rules.md)
- [Food_Delivery_Vision_and_Scope.md](Food_Delivery_Vision_and_Scope.md)
- [SRS_FoodDelivery.md](SRS_FoodDelivery.md)
- [SRS_SequenceDiagrams.md](SRS_SequenceDiagrams.md)
- [USE_CASE_SPECIFICATION.md](USE_CASE_SPECIFICATION.md)
- [Utility-Tree-ASRs.md](Utility-Tree-ASRs.md)
- [ASR-ADD-SAD/14 Quality Attribute.md](ASR-ADD-SAD/14%20Quality%20Attribute.md)
- [ASR-ADD-SAD/ASR_FoodDelivery.md](ASR-ADD-SAD/ASR_FoodDelivery.md)
- [ASR-ADD-SAD/ADD_FoodDelivery.md](ASR-ADD-SAD/ADD_FoodDelivery.md)
- [ASR-ADD-SAD/ADR_FoodDelivery.md](ASR-ADD-SAD/ADR_FoodDelivery.md)
- [ASR-ADD-SAD/SAD_FoodDelivery.md](ASR-ADD-SAD/SAD_FoodDelivery.md)

## Final Submission Statement

Traceability reviewed using evidence-based lean mapping
